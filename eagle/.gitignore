vendor/virtualagc/
vendor/Apollo-11/
build/
traces/
client/node_modules/
client/dist/
runtime/target/
# Defense in depth: AgcSession pins the child's cwd to build/agc/ so
# yaAGC's "core" erasable-memory checkpoint lands there, not here.
runtime/apps/eagle-runtime/core
