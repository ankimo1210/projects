# Wave 2 M1b — the P66 rate loop, 2026-07-31

Work on ledger open item 1 ("P66's rate loop"), from
`docs/superpowers/notes/2026-07-26-m1-pdi-flight.md`. Plan:
`docs/superpowers/plans/2026-07-31-eagle-m1b-p66-rate-loop.md`.

**Headline: `TAUROD` is pad-loaded at b=14 and the AGC reads it at b=11,
so it has been flying a rate loop at 0.1875 s instead of the intended
1.5 s — 8× too fast against the rope's own 0.2 s engine lag. MEASURED
2026-07-31 by an open-loop step test on a frozen plant (§7c); the shipped
b-scale rests on a premise this repo's own spike-B measurement had already
contradicted (§1). Neither the six existing flight traces nor two new
flights could settle it — that failure is structural (§3, §6) and is why
the step test exists.**

## 1. The defect in the shipped scale

`scenarios/p66-padload.toml` carries two comment blocks fifteen lines
apart that contradict each other.

Lines 252-259 derive the whole P66 cluster from a global premise:

> velocities in this file are at 2^10 m/cs (VIGN literal, DDUM denominator
> comment) […] `TAUROD = vel/acc -> fraction = tau_cs / 2^14` (b=14,
> CENTISECONDS)

Lines 265-274 — added **later**, from the spike-B live read-back, and the
only entry in `padload::P66_BSCALE_TABLE` marked `Verified` by
measurement — pin the two words that actually appear in TAUROD's divide:

> VDGVERT and HDOTDISP are DP b=7 in m/cs: HDOTDISP read back as
> `hi=0o36` = 491520 pulses while N63 R2 displayed +00756 (75.6 ft/s), and
> `491520 · 2^-21 m/cs` = 0.2344 m/cs = 76.9 ft/s

The rope's force law divides *those two words*, not `VBRFG`/`VIGN`
(`vendor/virtualagc/Luminary099/LUNAR_LANDING_GUIDANCE_EQUATIONS.agc:1041-1044`):

```
:1041  STODL  HCALC1
:1042           HDOTDISP
:1042  BDSU   DDV
:1043           VDGVERT
:1044           TAUROD
```

**TAUROD was never re-derived after the spike-B measurement landed.** The
b=14 word is 150 cs = 1.5 s only if the numerator is at b=10; at the
measured b=7 it is not. That much is certain regardless of what the right
answer turns out to be.

## 2. What the right scale is — NOT resolved

`b_TAUROD = b_vel - b_acc`, and `b_vel = 7` is measured. The acceleration
side is not pinned, because the path from TAUROD's quotient to a quantity
of known scale passes through two unresolved shifts, each worth exactly
one power of two:

- **`BDDV` by `22D`** (`:1051-1058`). `22D` is a direction cosine built by
  `DOT` of a `CDU*NBSM`-transformed `UNITX` with `14D`. The interpreter's
  `DOT` carries an inherent factor of two for unit vectors — note the
  explicit `SL1` immediately after the `DOT` at `:928` — so whether
  `/AFC/` and the pre-`BDDV` sum share a scale depends on a convention not
  established here.
- **`SR2`** on the gravity path (`:1047`), between `DDV GSCALE` and the
  `DAD` that adds the two terms at `:1050`.

Candidate answers and what the shipped 150 cs word decodes to:

| true b | AGC reads | factor vs intended 1.5 s |
|---|---|---|
| 11 | 0.1875 s | 8× too fast |
| 12 | 0.375 s | 4× too fast |
| 14 (shipped) | 1.5 s | correct |

Either error is a bang-bang oscillator against the rope's own 0.2 s engine
lag (`THROTLAG`, `CONTROLLED_CONSTANTS.agc:134`), which is the observed
symptom. **The direction of the error is certain; the factor is not.**

Anchors that *are* solid and should not be re-derived:

- `MASS` b=16 kg — `ERASABLE_ASSIGNMENTS.agc:1698`,
  "`# (1) MASS AFTER STAGING, SCALE AT B16 KG`".
- `GSCALE` = `2DEC 100 B-11` (`:1477`) — 100 cs at b=11, a DP time in
  centiseconds in the same velocity÷time→acceleration role.
- `/AFC/` at b=-4 m/cs² — `THROTTLE_CONTROL_ROUTINES.agc:206`,
  "MASSMULT SCALES ACCELERATION, ARRIVING IN A AND L IN UNITS OF
  2(-4) M/CS/CS"; `ABRFG` annotated `B+04`.

The gap is only *where in the chain* the b=-4 quantity sits relative to
TAUROD's quotient.

## 3. Why the existing traces cannot settle it

Two methods were tried against `build/traces/telem-m1-run{4,5,6}.jsonl`.
Both failed, and the failures are informative enough to record so nobody
repeats them.

### 3a. Differencing the throttle command against HDOTDISP

`rod_fit`'s original method. `VDGVERT` is not telemetered, but differencing
across a display repaint cancels it *and* the gravity term, leaving
`Δ(a_cmd·cosθ) = -(1/τ)·ΔHDOTDISP`.

Measured:

```
telem-m1-run4.jsonl: tau = 27.0425 s  (slope -0.0370 1/s, r2 0.153, n 36,  75 unsaturated repaints)
telem-m1-run5.jsonl: tau = 27.8745 s  (slope -0.0359 1/s, r2 0.047, n 107, 198 unsaturated repaints)
telem-m1-run6.jsonl: tau = 35.4682 s  (slope -0.0282 1/s, r2 0.040, n 101, 188 unsaturated repaints)
```

r² of 0.15 / 0.05 / 0.04 — **no fit.** The plan's kill criterion (r² ≥ 0.5)
was written before the numbers were seen, and it fired.

**Cause:** the method needs `VDGVERT` constant across each difference, and
`scenarios/pdi-descent.toml` drives a ROD *schedule* (run 4's
`[[245,-5.3],[50,-0.3],[12,0.7]]`). The sim is clicking `VDGVERT`
throughout the entire P66 window, so the cancellation never happens. A
direct look at run 4's repaint samples confirms it: between TIG+992.6 and
TIG+999.6 the command rises 4.2 → 31.0 kN while `HDOTDISP` also rises
−8.11 → −6.10 m/s, which the law forbids at constant `VDGVERT` — the error
term would be shrinking, not growing.

### 3b. Reading MINFORCE off the command floor — DISCARDED

The command floor looked like a `MINFORCE` clamp at half its loaded value:
run 4 bottoms at 178 bits = 2231 N against a pad-loaded 4560 N, and
`4560/2 = 2280 N = 182 bits` sits between run 4's minimum (178) and its 1st
percentile (187). That would have pinned the force words, and with
`b_force = b_MASS + b_acc` it would have pinned TAUROD without resolving
either shift above.

**It is not a clamp.** Histogramming run 4's low tail shows a smooth
distribution — about 10 frames (one guidance cycle at 10 Hz telemetry) at
each of ~40 consecutive bit values, 178 through 259, with no spike
anywhere:

```
  178 bits =  2230.7 N   10
  183 bits =  2293.3 N   10
  187 bits =  2343.5 N   10
  ...                    ...
  255 bits =  3195.6 N   21
  258 bits =  3233.2 N   20
```

A clamp produces a mode; this is a controller sweeping through a range.
Independently, `throttle_cmd_pulses` is our accumulation of the DINC
stream, and the rope's `PIFPSET` drive-past bookkeeping
(`THROTTLE_CONTROL_ROUTINES.agc:99-119`) offsets it from `FC`, so its
absolute level cannot measure a force word in any case.

**What the same data does establish:** runs 4 and 6 hit exactly 4096 bits
= `FEXTRA` (`THROTTLE_CONTROL_ROUTINES.agc:226`, `BIT13`, "4096 PULSES"),
the rope's own full-throttle drive-past constant. Landing on it exactly
validates the bit→newton mapping and the telemetry chain end to end.

## 4. What was built to settle it

- **`runtime/apps/eagle-runtime/src/bin/rod_fit.rs`** (`4c55466d`,
  extended `3eaed239`) — offline estimator, 5 tests.
- **`rod_clicks_cum` in the telemetry wire** (`24d81e56`) — the sim issues
  every ROD click, so it reports the running total and the analysis
  reconstructs `VDGVERT` exactly, at the live-verified 1 ft/s per click.
  Additive field; `SCHEMA_VERSION` stays 2. The handover's selection click
  is issued by `headless.rs`, not `phase8_rod`, and is booked from the
  same latch that triggers it.
- **Click-aware level regression** (`3eaed239`) — with
  `VDGVERT(t) = VDGVERT₀ + k·Δclicks`, the law is a straight line in the
  fully-observed regressor `x = k·Δclicks − HDOTDISP`, so `τ = 1/slope`
  and no constant-`VDGVERT` segment is needed. Recovers τ at 0.1875 /
  0.375 / 1.5 s to within 0.01 s *through* a `VDGVERT` change, so the
  candidate scales are separable.
- **`EAGLE_TELEM_OUT` / `EAGLE_ATT_DEBUG` fail loudly** (`259f6af2`) —
  both did `File::create(p).ok()`. The runtime's cwd is `runtime/` (the
  Makefile does `cd runtime && cargo run`), so the natural
  `EAGLE_TELEM_OUT=build/traces/x.jsonl` from the repo root resolves under
  `runtime/` to a directory that does not exist, and the instrumentation
  silently did nothing. **This cost a full 20-minute descent on
  2026-07-31** before it was noticed. Use absolute paths.

## 5. Flight 7

Flown **unchanged** — the pad load was deliberately NOT corrected first.
Flight 7's job is to measure τ, and it can only do that while the AGC is
still flying the word whose scale is in question.

```
[accept] class Crash v_vert 29.23 m/s v_horiz 72.04 m/s tilt 13.3 deg miss 13388.8 m
[accept] alarm episodes []; PROG lamp frames after ignition 41
[accept] AGC clock 0.944x real time (drift -67480 ms over 1208.0 s sim); sim pacing lost 0 ms; downlink 47.3 wps
scenario: done — MM ["00", "63", "64", "66"], touchdown Some(Crash), descent Some(862.6) s
```

| # | Build | ENGINE ON | MM seen | Outcome | v_vert | v_horiz | tilt | descent |
|---|---|---|---|---|---|---|---|---|
| 7 | unchanged pad load + `rod_clicks_cum` | t=344 s | `00`,`63`,`64`,`66` | **Crash** | 29.23 m/s | 72.04 m/s | 13.3° | 862.6 s |

Comparable to run 6 (30.86 / 60.04 / 12.8°), as expected — nothing about
the loop was changed. The limit cycle is intact.

**The flight did NOT produce the measurement, and the reason is a new
blocker.** `HDOTDISP` froze 1.6 s into P66 and never moved again:

| run | P66 frames | duration | distinct `HDOTDISP` | repaints/s |
|---|---|---|---|---|
| 4 | 2264 | 226.3 s | 110 | 0.49 |
| 5 | 2100 | 209.9 s | 206 | 0.98 |
| 6 | 2163 | 216.2 s | 228 | 1.05 |
| **7** | **2220** | **222 s** | **6** | **0.02** |

Run 7's entire P66 display history is three values, all inside the first
1.6 s:

```
  t=  986.1s  hdot= -3.2918  alt=  244.9  pulses= 1141
  t=  986.8s  hdot= -2.9566  alt=  243.3  pulses= 1015
  t=  987.7s  hdot= -2.8651  alt=  241.2  pulses=  715
```

`agc_alt_m` froze with it (5 distinct values against run 5's 108), so it
is the whole flight display, not one register. The AGC itself kept flying
— the throttle command ranged 232 to 2495 bits for the remaining 216 s.

**Mechanism, from this repo's own documentation.** `runner.rs:236-243`
records that P66's `VERTDISP` repaints V06N60 every guidance pass
(`LUNAR_LANDING_GUIDANCE_EQUATIONS.agc:899-900`), and that a load typed
into that repaint stream "is rejected mid-sequence with OPR ERR and the
KEY REL lamp lit, leaving RODCOUNT unwritten and VDGVERT unmoved
(spike-B iter 18)". `rod_load` guards against this with
`grab_dsky` (KEY REL, then wait for the repaint burst to subside), but the
guard is best-effort — `wait_for_display_gap` gives up after 1.2 s.

The freeze timing matches the ROD loads exactly:

```
  click change ->  -1 at t=986.1s (alt 244.9)   <- handover selection click
  click change ->  -6 at t=988.2s (alt 239.9)   <- schedule batch
  display froze at t=987.7s
  ... 216 s with no display update, no further clicks ...
  click change ->   0 at t=1204.6s (alt 39.7)
  click change ->   2 at t=1205.7s (alt  9.1)
```

**Two consequences, and the second is worse than the first.**

1. The display is dead, so `agc_hdot_ms` is unusable and neither fitting
   method can run (`rod_fit` reports 3 unsaturated repaints, no fit).
2. **If a `rod_load` was swallowed, its clicks never reached RODCOUNT** —
   so `rod_clicks_cum` would report clicks the AGC never applied, and
   `VDGVERT` would not be where the count says. That breaks the click-aware
   fit's core assumption independently of the display. Flight 7 also
   counted **41 PROG lamp frames** after ignition, against 0 on runs 4
   and 6, which is consistent with something having gone wrong in the
   dialog — but the code is unknown for the same reason as ledger item 2:
   nothing reads FAILREG after ENGINE ON.

**This is not a regression from this session's changes.** The binary that
flew carried only `rod_fit` (a separate bin), the `rod_clicks_cum` counter
(`sim.rs` `tick`, a pure accumulator), and the env-var panics (startup
only) — none touches `rod_load`, the script pump, or the DSKY. Run 4's
0.49 repaints/s against runs 5 and 6's ~1.0 suggests the same failure
partially present earlier and never noticed.

**Next flight must capture `--trace-out`.** Run 7 did not, so the KEY REL
and OPR ERR lamp states — which would confirm or refute the swallow
directly — are not recoverable from it. That is the single cheapest thing
that would settle it.

## 5a. What to fix before flight 8

Ranked, and none of it costs a flight to attempt:

1. **Verify the ROD load landed.** `rod_load` writes RODCOUNT and returns
   without checking. A read-back races RODCOMP (which zeroes the word
   within one 1-second pass — `runner.rs:233-234`), but `VDGVERT` itself
   is stable and readable. Confirming the commanded rate actually moved
   would turn a silent failure into a loud one.
2. **Re-establish the flight display after a load.** If the swallow leaves
   KEY REL latched, one more KEY REL would restore VERTDISP. Today nothing
   does, so a single failure blinds every downstream measurement for the
   rest of the run.
3. **Capture `--trace-out` on every instrumented flight**, so lamp state is
   always recoverable after the fact.

## 6. Flight 8 — the display fix works, and the measurement is still impossible

Flown after `a984c46a` (rejection detection + `release_dsky` +
accepted-only click counting).

```
[accept] class Crash v_vert 36.81 m/s v_horiz 64.18 m/s tilt 12.6 deg miss 12808.4 m
[accept] alarm episodes []; PROG lamp frames after ignition 0 pre-contact, 0 post-contact
[accept] AGC clock 0.944x real time (drift -67800 ms over 1212.3 s sim)
scenario: done — MM ["00", "63", "64", "66"], touchdown Some(Crash), descent Some(867.7) s
```

| # | Build | MM seen | Outcome | v_vert | v_horiz | tilt | descent |
|---|---|---|---|---|---|---|---|
| 7 | unchanged | `00`,`63`,`64`,`66` | Crash | 29.23 m/s | 72.04 m/s | 13.3° | 862.6 s |
| 8 | + ROD-load fixes | `00`,`63`,`64`,`66` | Crash | 36.81 m/s | 64.18 m/s | 12.6° | 867.7 s |

Still a crash, as expected — **nothing about the loop's gain was changed**,
only the instrumentation and the DSKY handover. The limit cycle is intact.

**The PROG alarms went away.** Flight 7 counted 41 lamp frames after
ignition; flight 8 counts **0 pre-contact and 0 post-contact**. That is
consistent with flight 7's alarms having been a downstream symptom of the
refused ROD load and the stuck display, not of the descent itself — and it
is the first run in this project's history to come back clean on that
counter with the split window in place.

**The display blindness is fixed.**

| run | P66 duration | distinct `HDOTDISP` | repaints/s |
|---|---|---:|---:|
| 5 | 211.9 s | 206 | 0.97 |
| 6 | 218.3 s | 230 | 1.05 |
| 7 | 221.9 s | **6** | **0.03** |
| **8** | **215.4 s** | **231** | **1.07** |

Flight 8 is the best of any run, and **zero ROD loads were rejected** —
the log's `REJECTED` / `GAVE UP` counters are both 0, so `rod_clicks_cum`
is trustworthy for the first time. `rod_fit` gets 160 unsaturated samples
against flight 7's 3.

**And the τ measurement still cannot be made from a descent.** This is not
an instrumentation problem, and no better estimator will fix it.

Three estimators were tried, all on good data:

| method | τ | r² |
|---|---|---|
| differencing across repaints | 12.94 s | 0.088 |
| click-aware level regression (PRIMARY) | 18.66 s | 0.042 |
| truth `vz` at 10 Hz, best command lag | 4.57 s | 0.63 |

The third looks like it worked. It did not. Sweeping the assumed command
lag shows r² is a **periodic function of the lag**, peaking and collapsing
on a fixed cycle:

```
 lag_s     tau_s      r2
   0.0    61.459   0.003
   2.0     6.434   0.297
   4.0     4.422   0.634
   5.0     4.396   0.641   <- peak
   6.0     4.818   0.534
   8.0     8.136   0.188
  10.0    65.859   0.003   <- collapse
  14.0       nan   0.184   <- rising again
```

The limit-cycle period, from `vz` zero crossings, is **19.4 s** — a
quarter period of **4.9 s**. The r² peak sits at **5.0 s**. A
quarter-period shift is exactly what maximizes the correlation between an
oscillation and the response driving it, and the collapse at 10 s is the
half-period anti-correlation. **The regression is measuring the limit
cycle's own geometry, not the control law**, and its "τ" tracks the period
rather than the gain — which is also why τ shrank monotonically as the
assumed lag grew.

**The root cause is structural: a saturated relay limit cycle carries
almost no information about the linear gain inside it.** The loop spends
its time at the stops, crosses the linear band quickly, and every signal
is dominated by phase. All three failures — §3a, the r²=0.04 level fit,
and this one — are the same fact seen three ways.

## 7. What will actually work: an open-loop step test

Stop trying to identify the loop from a descent. Do to `TAUROD` what
spike B did to `RODSCALE`: a short, controlled, live calibration.

`tests/live_spike_p66.rs` already boots to P66 at a benign state and reads
`cmd_pulses` (`:152`). The experiment:

1. Reach P66 and let the rate settle somewhere well inside the throttle
   band — no saturation at either stop.
2. Step `VDGVERT` by a known number of clicks (`rod_load`, now verified to
   land, at a live-confirmed 1 ft/s each).
3. Measure the resulting **step** in commanded thrust before the vehicle's
   own rate responds.

Then `τ = Δ(rate error) / Δ(a_cmd)` directly, from a step whose input is
exactly known and whose output is a single algebraic difference. One clean
step separates 0.1875 / 0.375 / 1.5 s by a factor of 4 to 8; no regression,
no phase, no saturation.

This costs roughly 10 minutes rather than a 20-minute descent, and it is
the same pattern this repo already trusts for `RODSCALE` — which is the
one b-scale in `P66_BSCALE_TABLE` marked `Verified` by measurement.

## 7a. Step test, attempt 1 — the machinery works, the testbed does not

Built as `runtime/apps/eagle-runtime/tests/live_rod_step.rs` (`dfe81515`)
on the spike-B hover choreography. Run 2026-07-31, 418 s:

```
[step] P66 working point: cmd 295 bits (range 262..1243 over 10 s), vz -47.067 m/s, alt 2121.2 m
[step] clicks -1  cmd 295 -> 347 bits (+52)  vz -47.067 -> -68.465 m/s  mass 15133 kg
[step]   dVDGVERT -0.3048 m/s, dHDOT -21.3982 m/s, d(a_cmd) +0.0431 m/s^2  => TAUROD = 489.8426 s
panicked: the AGC refused the step's ROD load (EntryStatus { key_rel: false, opr_err: true })
```

**Two results, one good and one bad.**

**Good: the rejection detection fired, for the first time ever.** The
second ROD load was refused with `opr_err`, and the test stopped instead
of quietly computing a number from a step that never happened. Before
`a984c46a` this run would have produced a plausible-looking τ from a
`VDGVERT` that never moved. That is precisely the failure mode the
detection was added for, and it caught it on its first live outing.

**Bad: the spike-B hover is not a benign testbed, and the premise behind
choosing it was wrong.** At P66 entry the vehicle is at 2121 m falling at
**−47 m/s**, and over the 5 s step window it accelerates to −68 m/s. The
command sits at 295 bits — at the idle stop — and swings 262..1243 over
the preceding 10 s, so it is neither trimmed nor settled.

The measurement is therefore meaningless: `dHDOT` (−21.4 m/s) swamps
`dVDGVERT` (−0.30 m/s) by **70×**, and τ = 490 s is that ratio, not a time
constant. Free fall is inherent to the choreography — P63 parks the
throttle at the DPS idle stop (4560 N) for the whole ZOOMTIME trim phase
against a 15 195 kg vehicle's 24 616 N weight, i.e. −1.32 m/s² for ~30 s
before P66 can take over. The "benign hover at 3000 m" in the test's
header was an assumption, never checked.

**The deeper problem this exposes: the step must dominate the transient,
and no closed-loop state in this project provides that.** τ is recoverable
only when |ΔVDGVERT| ≫ |ΔHDOT|. In the hover spike the vehicle is in free
fall; in the PDI descent P66 limit-cycles from the moment it takes over —
*because* τ is 4-8× too small. Waiting for a quiescent window is circular:
the loop is not quiescent precisely because of the defect being measured.

## 7b. The fix: freeze the plant, not the loop

Make the test genuinely **open-loop**, which attempt 1 claimed to be and
was not. Hold the vehicle's specific force at exactly lunar g so the AGC's
own altitude rate is constant by construction — then `ΔHDOT ≡ 0` and

```text
  TAUROD = dVDGVERT / d(a_cmd)
```

exactly, with no transient to dominate and no dependence on nav-vs-truth
agreement.

The piece already exists: `SyntheticHover::spawn` is the v1 feeder, "a
constant specific force of +1.62 m/s² along SM +X (lunar-surface hover)"
(`runner.rs`), used from boot until ignition. What is missing is a mode
that feeds that constant hover stream **while still tracking the AGC's
`cmd_pulses`** — today `spawn_closed_loop` does the tracking but also
integrates the vehicle, and `spawn` does neither.

That is a small addition to `runner.rs`: a closed-loop variant whose plant
is frozen. With it, one click gives `d(a_cmd) = 0.3048/τ` directly —
1.63 m/s² at b=11, 0.81 at b=12, 0.203 at b=14 — separable by 2× with no
correction term at all.

## 7c. MEASURED: `TAUROD` is b=11 — the AGC reads 0.1875 s

Step test attempt 2, on the frozen plant (`f7296ee5`), 2026-07-31, 380 s.
The freeze held exactly — `vz 0.000 m/s, alt 3000.0 m` for the whole run —
so the AGC integrated a vehicle whose altitude rate never changed.

```
[step] P66 working point: cmd 0 bits (range 0..373 over 10 s), vz 0.000 m/s, alt 3000.0 m
panicked: THRUST DINC loop never moved the actuator off its zero stop
```

**The test failed on a guard, and the guard's own value is the
measurement.** `STARTP66` initialises `VDGVERT` from the current altitude
rate (`LUNAR_LANDING_GUIDANCE_EQUATIONS.agc:157`, `DXCH VDGVERT #
ALTITUDE RATE`), so after the −1 selection click the rate error is exactly
**−0.3048 m/s**, and

```text
  a_cmd = g − 0.3048 / TAUROD
```

with every other term known. At 15 195 kg and g = 1.62 m/s²:

| τ | b | a_cmd (m/s²) | F_cmd (N) | bits |
|---|---|---:|---:|---:|
| 1.5 | 14 (as shipped) | 1.417 | 21 528 | **1718** |
| 0.375 | 12 | 0.807 | 12 265 | **979** |
| 0.1875 | 11 | −0.006 | −85 | **zero stop** |

**Observed: 0 bits.** The command drove all the way to its zero stop,
which requires

```text
  g − 0.3048/τ ≤ 0   ⇒   τ ≤ 0.3048/1.62 = 0.1881 s
```

and b=11 predicts **0.1875 s** — a **0.3 %** match. The weaker bound, taking
the pad-loaded `MINFORCE` (4560 N) as the clamp instead of zero, is
τ ≤ 0.2309 s, which still excludes both other candidates.

**What this does and does not establish.** The measurement is one-sided:
it bounds τ from above and cannot by itself exclude a scale *smaller* than
b=11. It is the derivation that bounds the candidate set from below —
`b_TAUROD = 7 − b_acc` with `b_acc` either −4 (the rope's own
`THROTTLE_CONTROL_ROUTINES.agc:206`) or −5 (if the `BDDV` by the
`DOT`-built cosine shifts one power of two), giving 11 or 12 and nothing
else. **The derivation narrows it to {11, 12}; the measurement excludes 12.
Together they pin b = 11.**

That is also the first independent confirmation that this project's model
of the P66 force law is right at all: three regressions had failed to
show the law describing the data, and here a first-principles prediction
of the commanded throttle came out on the nose.

**Consequence.** `scenarios/p66-padload.toml` ships `TAUROD` at b=14, so
the AGC decodes the intended 150 cs as **18.75 cs = 0.1875 s** — an **8×
too-fast rate loop** against the rope's own 0.2 s `THROTLAG`. That is the
limit cycle.

## 7d. Flight 9 — the limit cycle is GONE, and the blocker moved to item 3

First flight on the corrected `TAUROD` (b=11, `f10dc5d5`).

```
[accept] class Crash v_vert 154.07 m/s v_horiz 67.84 m/s tilt 13.0 deg miss 19721.1 m
[accept] alarm episodes []; PROG lamp frames after ignition 0 pre-contact, 0 post-contact
scenario: done — MM ["00","63","64","66"], touchdown Some(Crash), descent Some(940.7) s
```

**The touchdown number is worse and the flight is better.** Read the P66
segment, not the impact: over the first 85 s, like for like,

| run | cmd bits | vz range (m/s) | \|vz\| p90 | alt at +85 s |
|---|---|---|---:|---:|
| 7 | 232..2495 | −5.7 .. **+10.7** | 8.54 | **318.0 m** |
| 8 | 220..2505 | −4.5 .. **+12.4** | 8.83 | **339.9 m** |
| **9** | **260..1981** | **−5.9 .. +2.6** | **5.33** | **40.6 m** |

Runs 7 and 8 entered P66 at ~246 m and **climbed** — porpoising ±10 m/s
while the throttle slammed toward its stops. **Flight 9 descended**, 246 m
to 40.6 m, with the command staying inside 260..1981 bits (never near the
4096 drive-past), the sink rate confined to −5.9..+2.6 m/s, and the AGC's
own rate tracking truth closely the whole way (−0.21/−0.57, −2.29/−2.63,
−5.36/−5.31). **That is the first controlled P66 descent this project has
flown**, and it is exactly what removing an 8×-too-fast rate loop
predicts.

**What kills it is ledger open item 3, measured.** The AGC's altitude is
already **−188 m** in error at P66 entry and degrades steadily:

```
  t= 988.6  truth  246.2 m   agc    57.9 m   err  -188.3 m
  t=1018.6  truth  141.3 m   agc   -62.2 m   err  -203.5 m
  t=1048.6  truth   97.3 m   agc  -129.5 m   err  -226.9 m
  t=1063.6  truth   68.0 m   agc  -171.9 m   err  -239.9 m
```

The AGC therefore believes it is **170 m underground** while the vehicle is
still 68 m up. At t=1075 the ROD schedule's 40 m step fires (clicks −6 → 0)
and one pass later the AGC's displayed rate jumps from −5.5 to **−138.9
m/s** against a truth of −0.9; its altitude then runs away to −345, −620,
−887 m. Guidance commands `FEXTRA` (4096 bits) continuously, the vehicle
climbs to 7.3 km, and the 154 m/s impact is what falls back out of that.

The M1 ledger predicted this: *"it costs the AGC its altitude margin
before the terminal phase and is the reason P65 believes it has landed
while the vehicle is still 240 m up."* Its −190 m estimate is confirmed at
−188 m at P66 entry.

**So the blocker moved from open item 1 to open item 3.** Item 1 is
closed: `TAUROD` is measured, corrected, and the limit cycle it caused is
gone. Item 3 is now what stands between this project and a landing, and it
is a navigation problem, not a control one.

## 9. Open item 3, investigated — **see the correction in §9f before
## quoting anything in §9c**

Offline forensics on flight 9's traces (`telem-m1-run9.jsonl` +
`pkt-descent-full.jsonl`; tool: `scripts/downlink_nav_split.py`). Zero
flights spent.

### 9a. Hypotheses killed, with the measurements that killed them

- **The ledger's `v_h·θ` geometry hypothesis (flight 3) is REFUTED.** It
  predicts the radial-rate error shrinks with v_h, which falls 30×
  through P64 (219 → 6.5 m/s). Measured: the error rate is **constant**
  at −1.2..−1.4 m/s across that whole span; the implied θ would have to
  grow 5 → 151 mrad in 150 s. It is not a constant downrange offset
  turning horizontal speed into apparent sink.
- **Not the gravity model.** Both sides are pure central force: truth is
  `state::gravity` (μ/r²), the AGC's Average-G lunar path is `MUNGRAV`
  (`SERVICER.agc:1087-1097`, central; the oblateness block in `CALCGRAV`
  is Earth-only, branched around for the Moon at `:948-953`).
- **Not Average-G truncation.** Trapezoidal gravity over 2-s steps against
  RK4 at 10 ms differs by ~1e-6 m/s per step on this trajectory —
  ~3e-4 m/s over the descent.
- **Not the REFSMMAT seam, not the initial state.** `generate_state`'s
  REFSMMAT rows and the sim IMU's SM (≡ initial body attitude, the −90°
  X-rotation) are the **same rotation by construction**; the truth's
  ENGINE-ON state is the pad state's own gravity-only back-propagation,
  and the measured nav error at ignition is −3.8 m radial / −0.12 m/s.
- **Not a P63 accumulation.** The downlink decode (below) puts the
  **downrange** nav error at ≈ **−65 m** in late P63, after ~400 km
  flown — inertial navigation on this seam is working to ~1 part in 10⁴.

### 9b. The downlink decode — the AGC's own downrange, measured

Flight 9's `--trace-out` recorded every ch 034/035 downlink pair
(60,731 of them). The LM descent downlist layout (counted from
`DOWNLINK_LISTS.agc`, exactly 100 pairs / 2-s frame) carries `RGU`,
`VGU`, `LAND`, and the `RN`/`VN`/`PIPTIME` snapshot. Validation that
held: `LAND` decodes at b=24 to **|v| = R_SITE to the meter**, and the
truth's downrange is reconstructible from telemetry as
`θ(t)·R_SITE` with `θ̇ = −v_horiz/r`, anchored by re-running
`pdi_truth_state`'s back-propagation.

Measured (site-rotation anchor applied; see caveat):

| t (s) | phase | dH (m) | dD (m) |
|---|---|---:|---:|
| 745 | P63 late | ≈ −25 | ≈ −65 |
| 830 | MM64 | −57 | −563 |
| 873 | P64 | −95 | −568 |
| 917 | P64 | −111 | −620 |
| 939 | P64 | −146 | −698 |

(dH after the chord-curvature correction `√((RGU_X+R)² + RGU_Z²) − R`,
which is worth −600 m at 43 km downrange and reconciles the RGU numbers
with the HCALC display measurements exactly.)

**Caveat, flagged deliberately:** the absolute dD anchor depends on the
sign of the site-rotation correction `ω·(TLAND−t_eng)·R ≈ 3.2 km`, which
was flipped once during this analysis. The **slope** does not depend on
the anchor and is solid: **dD grows at ≈ −3.2 m/s through P64**, in
lockstep with dH's −1.3 m/s, both switching on around MM64.

### 9c. Where this leaves the diagnosis

A combined **~3.5 m/s velocity error (−3.2 m/s downrange, −1.3 m/s
radial) appears at/near the MM64 transition** and stays constant; the
position errors are its integrals. Before it, navigation is healthy;
after it, nothing further degrades. This is the signature of a
**discrete event or a maneuver-window effect**, not a continuous model
error — and the P64 entry is exactly where the rope repositions the LR
antenna, switches guidance targets, and pitches the vehicle over.

**The decisive next measurement is already in the trace:** the `VN`
snapshot in every downlink frame. If VN steps by ~3.5 m/s at one
PIPTIME near MM64 → a discrete state update (the LR-bypass seam is then
the prime suspect, because MM64 is when the radar would come alive).
If it ramps over the ~30 s pitchover → a sensing seam during the
maneuver. The pair-stream has drops that defeat index-based slot
extraction, so this needs the anchor-based method
(`scripts/downlink_nav_split.py`'s notes) built into a real decoder —
a Rust tool with tests, next session's first task.

## 9d. GATE 1 ANSWERED — it is a RAMP over the P64 pitchover, not a step

Tool: `runtime/apps/eagle-runtime/src/bin/downlink_dump.rs` (5 tests,
including the slip-rejection case that the Python spike failed). It
validated **144 frames** out of flight 9's 58,596 pairs — and the reject
tally is the point: `land 57767, clock 680, vgu 3, vn 0`. `LAND`'s
magnitude alone admits ~830 candidates; the AGC-clock-vs-wall-clock
cross-check is what removes the false anchors.

**The listing count was wrong and the stream corrected it.** Hand-counting
`LMDSASDL` put `TIME2` at −22 from `LAND`; the data says **−20**, pinned by
r = 1.0000 against wall clock. Every offset is now empirical:

| slot | offset | evidence |
|---|---|---|
| `TIME2` | −20 | r = 1.0000 vs wall clock, 147 frames |
| `VGU` | −3 | r = +0.99051 vs truth speed, 76 frames |
| `VN` | −17 | r = +0.99903 vs truth speed, 77 frames |

`RGU` and the `RN` snapshot did **not** resolve — no candidate beats
r = 0.54, and absolute-error testing cannot separate them because the nav
error under investigation is itself ~150 m. They are not decoded. Nothing
needed depends on them: `VGU`'s X component is radial-at-site and carries
the signal.

**A statistic that would have misled, recorded so it is not retried:**
`|VN|` shows no step at MM64 — but it *cannot*. A 1.3 m/s radial error is
perpendicular to a 200 m/s mostly-horizontal velocity, so it moves the
magnitude by 1.3²/(2·200) ≈ 0.004 m/s. Testing the wrong projection would
have "cleared" navigation.

**The measurement.** Radial-rate error (HDOTDISP vs truth, 10 Hz) across
the transition, with the attitude rate beside it:

```
      t  mm   tilt  pitch rate  err_hdot
  828.2  63   66.4    -0.560/s      0.35
  832.3  64   66.4    -0.900/s      0.43   <- MM64
  836.4  64   54.4     0.458/s     -0.18   <- 12 deg in 4 s
  840.5  64   53.8     1.417/s     -0.92
  844.6  64   53.6    -0.777/s     -1.01
  848.7  64   51.2    -2.707/s     -1.11
  ...            (attitude settles, error holds)
  889.7  64   39.8    -2.399/s     -1.17
```

The error moves from **+0.4 to −1.0 m/s over ~12 s (t = 832→844)**, exactly
across P64's pitchover, and then **stops growing and holds** once the
attitude rate settles. It is acquired *during attitude motion* and frozen
in as a velocity bias — not injected at one servicer pass.

**⇒ GATE 1 branch B: a maneuver-window sensing seam** (plan Task 2B). The
LR-bypass discrete-update hypothesis is dead; MM64 was a coincidence of
timing, not the cause — the pitchover is.

**Quantified target for Task 2B.** 1.4 m/s acquired over ~12 s =
**0.117 m/s² of spurious acceleration** against a specific force of
~2.9 m/s² — a **2.3 % projection error, i.e. ~1.3° of effective attitude
discrepancy** between the ΔV the sim applies to truth and the ΔV the AGC
integrates, present only while the vehicle is slewing.

One candidate is already excluded by arithmetic: `sim.rs::phase6_sensors`
rotates the whole tick's ΔV by the *end-of-tick* attitude while
`sf_body` was computed at the tick's start, but at 0.5°/s that is
4.5e-5 rad × 2.8 m/s² ≈ 1.3e-4 m/s² — **200× too small**. The suspect
list Task 2B should work, in magnitude order: CDU pulse timing/lag versus
PIPA delivery during a sustained rate (a one-servicer-pass attitude
staleness at the measured 3 °/s peak is ~1.5°, the right size); the
trim-gimbal thrust-direction lag; and the body-vs-SM projection seam in
`imu.rs`.

## 9e. Task 2B, first cut: the sim's accelerometer chain is EXONERATED

Three offline tests, no AGC and no flights
(`sim.rs::pipa_matches_truth_*`). Each flies `SimCore` under real thrust
and compares **the ΔV the PIPAs reported** against **the ΔV the truth
state actually gained**, minus trapezoidal gravity — i.e. exactly what the
AGC's navigation integrates versus what really happened.

| condition | measured discrepancy |
|---|---|
| holding attitude, 4 s | 0.0052 m/s = 0.0013 m/s² (PIPA quantization) |
| kinematic pitchover, 3 °/s, 12 s | 0.0109 m/s = **0.00091 m/s²** |
| RCS-driven pitchover (4-jet couple), 12 s | passes the same 0.05 m/s bound |

The seam being chased is **0.117 m/s²**. The sim's sensor chain is
**128× too small** to produce it, measured rather than argued. That
retires two of the three suspects outright — the body→SM projection and
the tick-rotation seam — and the RCS variant retires the "jets' force
reaches truth but not the accelerometers" shape too, which mattered
because RCS is the one force in the right magnitude band (445 N at ~15 t
= 0.03 m/s² per jet, so a four-jet couple is 0.12 m/s²).

**A vacuous pass caught in the act, recorded because it nearly landed.**
The first version of these tests set `core.act.thrust_n = 30_000.0`
directly. `phase3_throttle` recomputes `act.thrust_n` from
`thrust.cmd_pulses` every tick, so the vehicle actually flew at the DPS
idle stop and "PIPA agrees with truth" was 1.5 m/s ≈ 1.5 m/s — true, and
meaningless. The tests now assert the truth state gained a substantial
ΔV before comparing anything, and the RCS test asserts jets actually
latched. **Every one of these tests passed before the guard was added.**

**Where that leaves Task 2B.** If the accelerometers faithfully report
truth's non-gravitational ΔV, and the AGC integrates PIPA plus its own
gravity, then a 0.117 m/s² velocity-error onset cannot come from the
sensor model. What remains, in order:

1. **The gravity term.** Dismissed early on truncation grounds
   (~3e-4 m/s²) — but that argument covered only *integration error*, not
   a frame or scale difference in the AGC's `MUNGRAV` path. It has not
   been measured against the sim's `state::gravity`, and it is the only
   other input to Average-G.
2. **The trim gimbal.** `thrust_dir(trim_pitch, trim_roll)` sets the DPS
   thrust direction; the gimbal drives during P64. Both truth and PIPA
   read the same `act`, so a *lag* cannot split them — but a trim-driven
   change in thrust DIRECTION is exactly a maneuver-window effect, and
   the sim's trim rate versus the rope's expectation has never been
   checked.
3. **Authentic AGC behaviour.** Average-G is a 2-second rectangular
   integrator; the real Apollo navigation genuinely accrued error during
   maneuvers and the crew flew with it. If the residual is authentic, the
   fix is not to remove it but to land with it — which is precisely what
   the landing radar is for, and points at M3 rather than a bug.

The next measurement is (1): compare the AGC's own gravity increment
against the sim's over the same interval. `GDT/2` is on the descent
downlist, so `downlink_dump` can read it directly — no flight needed.

## 9f. CORRECTION to §9c — most of the "velocity error" is measurement artifact

§9c claimed "a combined ~3.5 m/s velocity error (−3.2 m/s downrange,
−1.3 m/s radial) appears at/near the MM64 transition." **That overstates
what the data supports, and the mechanism was wrong.** Reading the rope's
own definition of the signal is what corrected it.

**`HDOTDISP` is an extrapolation, not a state.**
`LUNAR_LANDING_GUIDANCE_EQUATIONS.agc:1015-1032`:

```
        PIPTIME
    STORE 30D        # TIME IN CS SINCE PIPTIME
    DDV   PDVL
        4SEC(28)
        GDT/2        # <- GRAVITY increment only
    VSU   VXSC
        VBIAS
    VSL2  VAD
        V
    STOVL 24D        # UPDATED VELOCITY
        R
    UNIT
    DOT   SL1
        24D
    STODL HDOTDISP
```

So `HDOTDISP = UNIT(R) · [V + (GDT/2 − VBIAS)·(t − PIPTIME)/4s]` — the
state velocity extrapolated from the last PIPA read **using gravity
alone**. It structurally omits the thrust accrued since `PIPTIME`, so the
displayed rate lags the true rate by the vehicle's own vertical
acceleration, and the lag's SIGN follows the sign of `dvz/dt`.

**That is exactly the "onset" §9c reported.** `dvz/dt` is negative through
P63 (the vehicle is still accelerating downward) and turns positive at the
P63→P64 transition (P64 starts arresting the descent). The error does not
switch on at MM64 — **it changes sign there**, because the quantity it
lags does.

Fitting `err_hdot = −(dvz/dt)·lag + bias` over t = 400–1000 s
(6000 samples):

```
  implied lag = 2.602 s      (2 s guidance period + ~1 s display repaint)
  bias        = -0.329 m/s
  r2          = 0.50
```

A 2.6 s effective staleness is exactly what the mechanism predicts, and it
accounts for about half the variance.

**What survives, and what does not:**

- **The −1.3 m/s radial "velocity error" does not survive** as a
  navigation defect. It is mostly the stale-display term.
- **The −3.2 m/s downrange figure does not survive either.** It rested on
  `RGU` decoding that the validated Rust decoder could **not** reproduce
  (best r = 0.54 across all candidate offsets, §9d). It should not be
  quoted until `RGU` is pinned.
- **The −190 m altitude error survives and is still unexplained.**
  Display staleness cannot produce it: at t = 960 the vehicle is doing
  −7 m/s, so 2.6 s of lag is 18 m against a measured −146 m.
- **The residual bias is suggestive.** −0.329 m/s sustained from ignition
  (t = 344) to P66 entry (t = 988) integrates to −212 m against the
  measured −184.5 m — the right order. But r² = 0.50 with only two
  regimes of `dvz/dt` leaves slope and intercept partly degenerate, so
  this is a lead, not a result.

**Excluded by measurement, not argument:** the AGC's lunar μ.
`CONTROLLED_CONSTANTS.agc:326` gives `MUM = 4.9027780 E8` in m³/cs²
(B-30), i.e. 4.9027780e12 m³/s², against `MU_MOON = 4.9028e12` — a
**4.5e-6** relative difference. The residual needs 3.15e-4. **70× too
small.**

**Method note worth keeping.** Three separate times this investigation has
been misled by the *instrument* rather than the system: a limit cycle's
quarter period masquerading as a control gain (§6), `|VN|` structurally
unable to see a perpendicular error (§9d), and now a gravity-only
extrapolation read as a state estimate. Read the rope's definition of a
signal before differencing it against truth.

## 9g. `RGU` does NOT decode — two methods tried, both failed

Pinning `RGU` would decompose the surviving −190 m position error into
radial and downrange parts, which is the next thing item 3 needs. It did
not work, and the second attempt produced a **spurious winner that only
structural checks killed** — worth recording so it is not re-derived.

The rope's definitions (`LUNAR_LANDING_GUIDANCE_EQUATIONS.agc:425-475`):

```
    RGU = CG * (R - LAND)      via  MXV CG / VSL1
    VGU = CG * (V - WM x R)    via  ANGTERM ("NO SHIFT SINCE ANGTERM IS
                                     DOUBLE SIZED") -> b=10 m/cs
```

`CG`'s first row is `UNIT(LAND)` (`CGCALC`, `:670-673`), so `RGU_X` is the
radial component of the position difference and `RGU_Z` is downrange —
hence the chord form `alt = hypot(RGU_X + R_SITE, RGU_Z) − R_SITE`.

**Method 1 — correlate decoded altitude with truth.** Useless here, and
the reason is worth stating: during a descent *everything* decreases
monotonically, so any monotone word correlates with altitude. The best
score (r = 0.94) landed on offset −1, which is `VGU_Z` — a velocity
component faking a position signal.

**Method 2 — require `d(RGU)/dt == VGU`.** A derivative relation, which
monotone signals cannot fake, against an operand (`VGU`) already validated
at r = 0.99. It produced a clear winner: **offset −7, b = 20, median
|d(RGU_X)/dt − VGU_X| = 0.030 m/s**, an order of magnitude better than any
other pair.

**It is wrong.** Decoding `RGU` there and checking the structural
invariants:

```
  median |RGU_Y| (crossrange, must be ~0 in-plane) = 493 749 m
  agc_alt at t=632.6 = 174 292 m   against truth 8 564 m
```

The derivative test had selected a small subset (27 pairs) through its own
`|rate| < 200` and `|x| < 4e4` filters, and inside that subset the
relation happened to hold. **A fit statistic on a filtered subset is not
evidence; the structural invariant is.**

`RGU` is therefore **not decoded**, and no radial/downrange decomposition
of the position error is available. `VGU`, `VN` and `TIME2` remain solid.
Note this is not a frame-drop problem: `VGU` sits at −3 and `VN` at −17,
so slots far *further* from the anchor decode fine. Either the b-scale is
outside 20..29, or `RGU`'s slot is not where the listing's ordering
implies — the same two-slot discrepancy §9d already found for `TIME2`
suggests the latter.

**Instrument-failure count for this investigation: four.** A limit
cycle's quarter period read as a control gain (§6); `|VN|` structurally
blind to a perpendicular error (§9d); a gravity-only extrapolation read as
a state estimate (§9f); and now a subset-fitted derivative relation read
as a slot identification. Every one produced a confident, plausible,
wrong number.

## 9h. Core-dump route: item 3 is a ~5e-4 delta-V deficit, present from
## ignition — and it is probably AUTHENTIC

Switching instrument settled in one flight what the downlink could not.
Flight 10 (`telem-m1-run10.jsonl`, Crash 25.29/51.79/9.0°) was flown with
a passive watcher copying `build/agc/core` on every rewrite: **121
snapshots of the AGC's entire memory**, 104 passing the `|LAND| = R_SITE`
validity gate, spanning PIPTIME 0–1278 s.

**The MM64 framing is dead.** Comparing the AGC's own `HCALC` against
truth — internal value against internal value, timestamped by the AGC's
own `PIPTIME`, with no display in the path:

```
  PIPT_s  mm     HCALC  truth_alt        dH    dH/dt
   387.9  63   15192.8    15192.9      -0.1   +0.116
   587.9  63    9427.5     9438.3     -10.8   -0.225
   747.9  63    6503.0     6565.6     -62.6   -0.356
   827.9  63    3918.8     4015.0     -96.3   -0.424   <- still MM63
   867.9  64    2282.1     2400.9    -118.8   -0.564
   987.9  64      58.2      258.9    -200.8   -0.686
```

The error accumulates **continuously from early P63** and is already
−96 m while still in MM63. There is no onset at MM64. §9c's "switches on
at MM64" and §9d's "ramp over the pitchover" were both reading the
display-lag term corrected in §9f.

**What it is.** The radial velocity error tracks the radial component of
specific force (r = −0.958 against `sf·cos(tilt)`), and expressing it as a
fraction of accumulated ΔV gives a **stable ~5e-4**:

```
      t      dV_r  int |a| dV   eps_total
   572.9   -0.2267      648.1     0.00068
   752.9   -0.4065     1272.1     0.00049
   992.9   -0.6840     1888.2     0.00047
```

The AGC loses about **0.05 % of its accumulated ΔV** over a 1888 m/s
burn — 0.9 m/s — and that integrates to the −190 m.

**Two sources excluded by measurement:**

- **The sim's PIPA output.** New regression test
  `pipa_has_no_accumulating_bias_against_truth` flies 300 s and bounds the
  reported-vs-truth ratio at **< 2e-5**. Quantization is bounded at one
  pulse; a scale error grows with ΔV; 300 s separates them. *This also
  corrects §9e's reasoning, though not its conclusion: the earlier tests
  did show this ratio (0.0052 m/s on 12.7 m/s = 4.1e-4) and it was
  dismissed as quantization because it was being compared against the
  inflated 0.117 m/s² target. The conclusion held; the argument for it
  did not.*
- **PIPA compensation.** `IMU_COMPENSATION_PACKAGE.agc:50-80` applies
  `DELVX = PIPAI + PIPAI·SFE − BIAS·Δt` from `PIPASCF`/`PIPABIAS`, neither
  of which this project pad-loads. Read straight out of the dump: **both
  are 0 on all three axes**, so the compensation is a no-op.

**[SUPERSEDED by §9i — this paragraph's conclusion is rejected]** What is left is the AGC's own Average-G integration — a 2-second
integrator on a powered trajectory — and a 5e-4 ΔV deficit is exactly its
character. **That is very likely authentic, not a bug.** The real Apollo
navigation accrued exactly this kind of error through the burn, which is
*why the LM carried a landing radar*: `DELTAH` and the LR updates existed
to correct the accumulated altitude before the terminal phase. Every run
in this project flies `lrbypass = true`, so nothing ever corrects it.

**This makes M3 the principled next milestone, not an optional one** —
see the road-to-landing plan's Task 6, which already recommended M3 first
on weaker grounds. Before committing to that, one cheap check remains:
quantify Average-G's own truncation on this trajectory offline and confirm
it lands near 5e-4. If it does, item 3 closes as "authentic; fixed by
radar, not by code."

**Instrument note.** The core dump also settled in one line two things the
downlink work could not: `RGU` is b=24 (so §9g's failure was a SLOT
problem, not a scale one), and `RN` is **b=29, not b=27** — |RN| decoded
at 27 gives 438 km, and ×4 is exactly the 1 752 608 m PDI orbital radius.

## 9i. REJECTED — Average-G cannot explain it. Item 3 is a real defect.

§9h ended with "very likely authentic, not a bug" and recommended M3 on
that basis. **That was wrong, and the cheap check that was supposed to
confirm it killed it instead.**

Estimated from the flown trajectory (no re-integration — the local error
of a trapezoidal step, summed analytically, over 431 guidance passes of
2 s across an 863 s burn):

```
  radial specific force  +0.085 -> +0.000 m/s^2
  POSITION truncation    -(dt^2/12) * d(a_radial)        = +0.03 m
  gravity 1.59615 -> 1.62432 m/s^2 (dg/dt = 3.27e-05)
  VELOCITY error from once-per-pass gravity              = +0.0282 m/s
    integrated over the burn                             = +12.2 m

  OBSERVED                                               = -200.8 m
  Average-G accounts for                                 =  +12.2 m
  shortfall                                              =    16x
```

**Sixteen times short, and the sign is opposite.** Average-G's finite step
is not the source. (An attempt to settle this by re-integrating the
trajectory two ways was abandoned first: without reproducing the guidance
loop the reconstruction diverges to a 820 km altitude, which is a broken
instrument, not a measurement. The analytic bound needs no reconstruction.)

**So item 3 is a real defect and the chain is now nearly closed by
elimination:**

| link | status |
|---|---|
| sim's PIPA output vs sim truth | **excluded**, < 2e-5 over 300 s (§9h) |
| PIPA compensation (`PIPASCF`/`PIPABIAS`) | **excluded**, both 0 on all axes, read from the dump |
| Average-G truncation | **excluded**, 12 m against 201 m, wrong sign |
| **the AGC not receiving all the ΔV the sim sends** | **the remaining suspect** |

A 5e-4 deficit is one lost pulse in 2000 — at ~300 pulses/s per axis,
one every ~7 s. Candidates: the socket path between the sim and yaAGC
dropping counter packets under load; or `SERVICER`'s read-and-zero of the
PIPA counters racing pulses that arrive mid-read.

**Next measurement, no flight needed for the first half.** Instrument the
sim to count PIPA pulses emitted per axis, telemeter the running total
(the `rod_clicks_cum` pattern), then difference the AGC's own `VN` between
two core-dump snapshots and compare against `pulses x PIPA_INCR` over the
same `PIPTIME` interval. Sent-versus-received, exactly, with both sides
measured. If they differ by 5e-4, the loss is in transport and the fix is
ours; if they agree, the deficit is inside the rope's own bookkeeping and
M3 becomes the answer after all — but on evidence, not on assumption.

**Note for the road-to-landing plan:** Task 6's "M3 first" recommendation
still stands on its original grounds (the real LM flew radar-corrected
navigation), but §9h's stronger claim — that item 3 *is* the reason —
must not be quoted. It is unproven and the arithmetic above is against it.

## 9j. Sent-versus-received: INCONCLUSIVE — the method is 5x too coarse

Flight 11 (Crash 18.08/62.69/12.1°) with `pipa_pulses_cum` telemetered and
121 core snapshots. The clean comparison avoids gravity entirely:
**`DELV`'s high word is the raw PIPA count for one servicer pass**
(`SERVICER.agc:570-580`; compensation is a no-op with `PIPASCF`/`PIPABIAS`
at 0), so it is directly comparable to the pulses the sim emitted in the
matching 2 s window.

Aggregated over 58 sampled passes:

```
  X: AGC   +11848   sim   +11769   rel +6.7e-03
  Z: AGC   -32417   sim   -32247   rel -5.3e-03

  Z per-pass difference: mean -2.93 pulses, sd 14.6, n=58
  standard error of the mean: 1.92 pulses on ~700/pass
  => method resolution:  2.7e-03 relative
     effect being hunted: 5.0e-04
```

**The method cannot resolve the effect — it is 5x too coarse**, and the
scatter is dominated by window alignment: the 2 s window is guessed as
`[PIPTIME-2, PIPTIME]` against the sim's wall clock, and at ~350 pulses/s
on Z a 100 ms misalignment is ±35 pulses.

Two things are worth keeping:

- **X and Z disagree in SIGN** (+6.7e-3 vs −5.3e-3). A transport loss —
  dropped counter packets — would be one-directional on every axis,
  because losing a pulse always reduces |ΔV| whatever its sign. This is
  evidence *against* the transport hypothesis, though at 2.5σ on a noisy
  statistic it is not conclusive either.
- **Nothing here shows a systematic one-directional deficit.**

**How to reach the needed resolution** (next session, no new mechanism
required): run yaAGC with `-dump-time=1` so *every* servicer pass is
captured instead of one in five, and pin the AGC-clock-to-wall-clock
offset from `TIME2` rather than assuming it — §9d's fit gave slope 1.0000
with 0.03 s residual, which is 10x tighter than the current alignment. At
~430 passes with tight alignment the standard error drops below 5e-4.

**Item 3 status: the cause is still not identified.** Excluded by
measurement: the sim's PIPA output, PIPA compensation, Average-G
truncation. Weakened but not excluded: transport loss. The -190 m is
real, reproducible across flights 9/10/11, and unexplained.

## 9k. Flight 12 aborted — `--dump-time=1` breaks the boot

The 9j plan was to capture every servicer pass by dumping erasable every
second instead of every ten. **It killed the run before ignition:**

```
  0: state pad-load
  1: pad-load word @02027
  2: V01N01 read of 02027 failed 3 times
```

yaAGC's core dump is a synchronous ~18 KB write inside its main loop
(`agc_engine_init.c:441-471` called from `SimManageCoreDump`), and at 1 s
it delays the AGC enough to blow `apply_padload`'s 3-attempt read-back
verification. 177 snapshots were captured, all of them pre-ignition.
Twenty minutes for no descent.

Reverted to opt-in (`EAGLE_DUMP_TIME`, unset by default). **The lesson is
the general one this session keeps re-learning: the instrument perturbs
the system.** Passive observation — the watcher copying a file yaAGC was
already writing — cost nothing. Changing yaAGC's own duty cycle is not
passive, and the boot choreography is the most timing-sensitive thing in
the run.

**Consequence for 9j's plan.** Per-pass capture via a shorter dump
interval is off the table unless it can be enabled *after* ignition, which
needs a control path to yaAGC that does not exist. The remaining route to
resolution is the other half of 9j: **better alignment at the existing
10 s interval.** With snapshots named by source mtime (already
implemented) the AGC-clock-to-wall fit tightens the window from ~100 ms
to ~10 ms, cutting per-sample noise from +-35 pulses to +-3.5; at 104
samples that is a standard error near 5e-4 — at the threshold rather than
5x above it. Marginal, but it costs one ordinary flight with no changes
to yaAGC's behaviour.

## 10. Still open

Ledger items **1a** (braking-gate residual, 911 m / 47 m/s), **2** (the
P65 PROG alarm code), **2a** (`prog_lamp_frames` counts through the ~2 s
post-touchdown tail — a known false negative in the frozen acceptance),
**3** (the −190 m P64 nav drift) and **4** (horizontal velocity in a
crewless P66) all carry forward untouched.

`LAG/TAU`, `MINFORCE` and `MAXFORCE` remain `Unverified` in
`P66_BSCALE_TABLE`. Nothing here pinned them — §3b is exactly why — and
`DPS_MIN_N` / `DPS_VE` / `RCS_THRUST_N` / `RCS_VE` are still
`lm_simulator.tcl` values and still on the suspect list.

## 11. Item 4 cannot close in a crewless P66 — and item 2 may be item 4's fix

Written 2026-08-01, before flight 18's result, so the reasoning is on the
record independent of how that flight lands.

**The arithmetic does not work.** The handover fires at 250 m, and the
measured state there is `v_horiz 5.8 m/s` (`scenarios/pdi-descent.toml:51`,
run 3). P66 holds attitude; it has no lateral channel and nobody flies the
RHC. So horizontal velocity at that gate can only grow. The acceptance's
`v_horiz_max` is **1.5** m/s and even the `Hard` class is 3.0 — 5.8 clears
neither. No ROD schedule fixes this, because ROD is the *vertical* channel.
Task 3's branch (a) "model a minimal crew" and (c) "accept Hard" were both
written before this number was in view: (c) is arithmetically dead, and (a)
means writing a guidance law of our own and calling it a crew.

**The rope already contains the right answer, and we are skipping it.**
P65 is the automatic landing phase, and its law is a **vector** one:

    ACG = (V2FG - VGU) / TAUVERT

(`LUNAR_LANDING_GUIDANCE_EQUATIONS.agc:911-922`, `P65VERT`: `VLOAD VSU /
V2FG / VGU / V/SC TAUVERT`). All three components are driven to the target
velocity. P66's law, fifteen lines below, is the scalar ROD one this
project has spent two sessions on — vertical only. **Nulling horizontal
velocity is structurally P65's job, and P66 cannot do it.** That is not an
inference from flight data; it is what the two routines compute. The scenario hands over at 250 m
specifically to jump over P65, because "this rope's P65 entry is where the
run dies (PROG alarm ...)" (`pdi-descent.toml:47-48`). That alarm is
ledger item 2 and **its code has never been read**.

**The hypothesis this raises: item 2 is a symptom of the bypassed radar.**
Every flight that met the P65 alarm flew with `lrbypass = true`. P65 is
the phase that most depends on the landing radar. If P65's entry alarm is
a radar-data complaint, then M3 — which is what this session has been
building — closes item 2, which unblocks P65, which nulls v_horiz, which
closes item 4. That is one fix for three open items, and it needs no crew
model and no threshold relaxation.

**It is a hypothesis, not a result.** It could equally be unrelated. What
makes it testable now and not before: `bin/agc_state` prints `FAILREG`
(three SP alarm-code registers, `ERASABLE_ASSIGNMENTS.agc:442`) out of the
periodic core dump, so the code can be read passively — no RSET, which
would clear it.

**Decision rule, fixed before the data:** once a flight reaches P64 with
the radar live, raise the handover gate out of the way (or remove it) and
let the rope enter P65. Then:
- No alarm, and P65 flies → item 2 was the radar; measure v_horiz at
  contact and re-run Task 3's gate against it.
- Alarm again → read `FAILREG` from the dump. The code names the
  complaint, which is more than any flight so far has produced, and item 2
  stops being "unknown" whatever the answer.

## 12. Flight 18 — defect 8 confirmed fixed; DEFECT 9 named

`build/traces/telem-m1-run18.jsonl`, `lrbypass = false`.

**Defect 8 is fixed, and the evidence is unambiguous.** Negating the
altitude count (HSCAL is negative) turned flight 17's runaway into a
normal braking phase:

| | flight 17 | flight 18 |
|---|---|---|
| P63 braking | climbed from ignition | 15.2 km → 1.2 km |
| `vz` after ignition | positive, to +2496 km | min −121.1 m/s |
| `nav_err_alt` | −394 → −89 930 m, monotone | ±3 km, oscillating |

**The flight still crashed** — `v_vert 119.59`, `v_horiz 770.04`, tilt
102.9°, miss 7969.6 m — because braking never finished: MM64 came at
t=789.4 s with the vehicle still at 1169.9 m doing −111.5 m/s, where a
healthy run reaches MM64 at ~1.5 km and hands over at 250 m / −1.7 m/s.
The cause is in the table above: the AGC's altitude is now *wrong by
kilometres in both directions* rather than diverging one way.

### Defect 9: the LR pad-load block was never loaded, and nothing declares the altitude scale

`SERVICER.agc:1136-1142`:

```
        CS      FLGWRD12        # TEST LR ALTITUDE SCALE FACTOR
        MASK    ALTSCBIT
        EXTEND
        BZF     +3              # BRANCH IF HIGH SCALE
        CA      SKALSKAL        # RESCALE IF LOW SCALE
        TC      SHORTMP
```

Two facts settle it:

1. **`SKALSKAL` is a pad load and we never load it.**
   `ERASABLE_ASSIGNMENTS.agc:813` — "LR ALT SCALE FACTOR RATIO: .2 NOM".
   `scenarios/p66-padload.toml:22-24` omits the whole LR block on the
   stated premise that "landing radar stays silent in Spike A (no LR
   data-good discretes), P65 is out of scope". **That premise stopped
   being true the moment M3 started driving the radar.** Also missing:
   `RADSKAL` (the Doppler bias, `SERVICER.agc:1134`), `DELQFIX` (the
   reasonableness test's tolerance), `V2FG` and `TAUVERT` (which are
   P65's *entire* guidance law — see §11).

2. **Nothing in the rope ever writes `ALTSCBIT`.** It is bit 9 of
   `FLGWRD12` = `RADMODES` (`FLAGWORD_ASSIGNMENTS.agc:1110-1112`), and
   `SERVICER.agc:1137` is its *only* reference in Luminary099. Checked
   Luminary131 and Luminary210: same — one read, no write. It is
   **external state the LR supplies**, and in this architecture the only
   thing that can supply it is our sim, which does not.

Fresh start zeroes the flagwords, so bit 9 is clear ⇒ **low scale** ⇒ the
AGC multiplies the reading by `SKALSKAL`, which is **zero**. The computed
slant range collapses, `DELTAH = 0 − HCALC`, and the AGC drives its own
altitude toward zero on every update. That is the ±3 km oscillation.

### The counter cannot represent the braking phase, either

`HSCAL` is 1.079 ft/bit (`CONTROLLED_CONSTANTS.agc:168`) = 0.3288792
m/count, and `RNRAD` is a 15-bit counter (±16383). So the largest
altitude the high scale can express is **5388 m** — and the low scale,
at ×0.2, only 1078 m. Flight 18 was asking it to carry 15 213 m
(46 257 counts). Even with the scale declared, altitude data-good must
not be asserted above the counter's range; this is not a workaround but
what the real LR's range gate does.

### Next actions, in order

1. **Declare high scale.** Set `FLGWRD12` bit 9 before the LR is first
   read. A blunt pad-load write would clobber live radar bits — the same
   hazard `p66-padload.toml:27-28` already documents for `FLAGWRD8` — so
   use the choreography's read-modify-write path, not a manifest word.
   High scale is the right choice: it needs no `SKALSKAL` at all, so it
   removes a multiply-by-zero instead of adding an unpinned b-scale.
2. **Gate `LR_RANGE_DATA_GOOD` on the counter's range** (|count| ≤ 16383,
   i.e. altitude ≤ 5388 m). Above that, assert velocity-good only.
3. **Load the rest of the LR block** — `RADSKAL`, `DELQFIX`, and (for
   §11's P65 experiment) `V2FG` and `TAUVERT`. Each needs its b-scale
   derived and cited before it is written, and `SKALSKAL` is only needed
   if step 1 is ever reversed.
4. Then re-fly with `lrbypass = false`.

`lrbypass` is back to `true` in `scenarios/pdi-descent.toml`: with the
scale undeclared the radar makes navigation worse than bypassing it, and
the acceptance scenario should sit in its best known state between
experiments.

## 13. Flight 19 — the decision rule fired; the split is NOT the LR scale, and NOT the counter windows

`build/traces/telem-m1-run19.jsonl`. Two provable defect-9 fixes were in:
`ALTSCBIT` declared read-modify-write in the pad-load choreography
(`runner.rs`, `FLGWRD12_ECADR` 0o110 / `ALTSCBIT` 0o400), and velocity
data-good gated on each beam's own counter window
(`lr::vel_in_counter_range`).

**Both fixes are correct and neither is the bug.** Flight 19 crashed at
`v_vert 154.14`, `v_horiz 587.79`, tilt 78.2°, miss 21 790 m, and never
reached MM64 at all (`MM ["00","63"]`).

The pre-stated rule was "track flight 11 past TIG+360 s". It did not:

| TIG+ | run 11 (healthy, bypassed) | run 18 | run 19 |
|---|---|---|---|
| 300 s | 8424 m, −12.2, 69.2° | 8431, −12.2, 69.8° | 8416, −12.4, 69.8° |
| 330 s | 8136 m, −12.4, 66.8° | 8149, −12.0, 65.1° | 8143, −11.2, 65.8° |
| **360 s** | 7623 m, −20.9, **62.1°** | 7565, −34.2, **39.7°** | 7618, −25.2, **112.5°** |

All three runs are the *same flight* to TIG+330 and diverge at TIG+360.
That is the fact to explain, and it now has three things ruled out:

- **Not the altitude path.** 8.1 km is far outside the counter's 5388 m
  window, so no altitude count is being sent there at all.
- **Not the read onset.** The packet trace shows the rope's first ch13 LR
  activity at **t = 328.65 s — fourteen seconds BEFORE ignition** — and
  767 activity packets over the run. The rope has been reading the LR for
  345 s by the time the runs split.
- **Not the velocity counter window alone.** Gating it changed the
  failure's shape (tilt went the other way, 112.5° instead of 39.7°) but
  not its onset, and made the run worse — dropping data the rope expected
  is itself a perturbation.

**New lead, unexamined: ch13 bit 7.** The distinct ch13 words in the
trace are `0o14 0o15` and `0o104 0o105 0o106 0o107 0o114 0o115 0o116
0o117`. The selects are the documented ones (`LRVELX` 0o14 … `LRALT`
0o17, `P20-P25.agc:2738-2755`), but **bit 7 (0o100) is set on most of
them and this project has never decoded it.** `decode_ch13` ignores it.
If bit 7 qualifies the select — a scale discrete, a different counter
destination, or a mode — then our responder has been answering a
question it did not fully read, for every flight in the M3 series.
**Decode ch13 bit 7 against the rope before flying again.**

Next cuts, in order:
1. Decode ch13 bit 7. This is free (grep the rope) and may explain
   everything above.
2. If bit 7 is inert: LR velocity data-good OFF entirely, altitude only,
   and see whether TIG+360 survives. That isolates the channel.
3. `lrbypass` is back to `true` meanwhile.

### 13a. ch13 bit 7 is a red herring — but the velocity read is FIVE SAMPLES

Bit 7 resolved, cheaply and negatively: `INITREAD` clears the select with
`CS ALLREAD / WAND CHAN13  # REMOVE ALL RADAR BITS` where `ALLREAD` is
`OCT 17` (`P20-P25.agc:2739, 2785-2792`). The radar bits are 1-4 only.
Bit 7 belongs to some other ch13 function, and `decode_ch13` is right to
ignore it.

**The real finding is in the same table** (`P20-P25.agc:2738-2760`):

```
LRALT           TC      INITREAD -1     # ONE SAMPLE PER READING.
ALLREAD         OCT     17
LRVELZ          TC      INITREAD        # <- no -1
                OCT     16
...
# LRVEL IS THE ENTRY TO THE LR VELOCITY READ ROUTINE WHEN 5 SAMPLES ARE
# WANTED.  ENTER WITH C(A)= 0,2,4 FOR LRVELZ,LRVELY,LRVELX RESP.
LRVEL           TS      TIMEHOLD
                CAF     FIVE            # SPECIFY FIVE SAMPLES
```

**LR velocity is a five-sample read; altitude and both RR quantities are
one-sample reads.** The rope accumulates into `SAMPLSUM`
(`P20-P25.agc:2798-2801`), and `RADSKAL`'s own comment is "SCALE TO RADAR
COUNTS **X 5**" (`SERVICER.agc:1134`) — the sum of five is what the
scaling expects.

`phase6b_landing_radar` drives `RNRAD` **once per ch13 rising edge**. If
the rope runs five sample gates per velocity reading, our responder
satisfies the first and leaves the counter holding a stale value for the
other four, so `SAMPLSUM` accumulates five copies of one sample instead
of five independent ones. That is a velocity error which is *silent while
the rope ignores LR velocity* and bites the moment it starts using it —
which is the TIG+360 s signature exactly.

This is the next thing to fix, and it is ahead of the "velocity off,
altitude only" bisection in §13, because it predicts that bisection's
result rather than merely measuring it. Read `INITREAD`/`LRVEL` fully
first: the open question is whether the five gates are five ch13 activity
cycles (which our rising-edge responder would already see) or five reads
inside one activity window (which it would not).

### 13b. The five samples are five activity cycles — the theory is WEAKENED, not confirmed

`P20-P25.agc:2899-2909`:

```
NOEND           TS      NSAMP
RESAMPLE        CCS     SAMPLIM         # SEE IF ANY MORE TRIES SHOULD BE MADE.
                TCF     +2
                TCF     DATAFAIL        # N SAMPLES NOT AVAILABLE.
                CAF     BIT4            # RESET ACTIVITY BIT.
                EXTEND
                WOR     CHAN13          # RESET ACTIVITY BIT
                TC      RESUME
```

**Each sample is its own ch13 activity cycle.** The read clears bit 4
(`P20-P25.agc:2873`) and `RESAMPLE` re-sets it for the next one, then
RESUMEs to wait for the next RADARUPT. So `phase6b_landing_radar`'s
rising-edge responder sees all five edges and recomputes from current
state each time — which is what a real LR would give. §13a's mechanism
does not hold as stated. Recorded so it is not re-derived.

**What §13a's reading DID surface, still unverified: the sign
convention.** `P20-P25.agc:2879-2885`, immediately after the velocity
select resolves:

```
                CAF     POSMAX
                MASK    RNRAD           # magnitude, 14 bits (POSMAX = 37777)
                AD      LVELBIAS
                TS      L
                CAE     RNRAD
                DOUBLE
                MASK    BIT1            # the sign bit, extracted separately
                DXCH    ITEMP3
```

The rope builds a **double-precision** reading whose high half is
`|RNRAD| + LVELBIAS` and whose low half is `RNRAD`'s sign bit recovered
through `DOUBLE / MASK BIT1`. `eagle_sensors::lr::vel_raw_counts` returns
a single signed count and the responder drives the counter with PINC /
MINC to reach it; **nothing in this project has ever checked that the
counter's sign, as the rope reconstructs it here, is the sign we
intend.** A sign error here is invisible while the rope ignores LR
velocity and appears the moment it uses it — which remains the TIG+360 s
signature.

Next: hand-decode this sequence for a known count (both signs), assert it
in `lr.rs` against `vel_raw_counts`, and only then fly. The bisection
(velocity off, altitude only) stays as the fallback if the decode is
clean.

### Decision recorded: item 4 closes through P65, not through a crew model

The user's call, 2026-08-01. `scenarios/pdi-descent-p65.toml` is the
vehicle for it: drop `[handover]` and let the rope fly its own automatic
landing. If P65's entry alarm returns, read the code from `FAILREG` via
the core dump rather than guessing — that is what §11 built the readout
for. The acceptance thresholds are not touched.

### 13c. The sign convention decodes CLEAN — inside the gated window

Hand-decoded, no flight needed. `DOUBLE` is `AD A`, and in 15-bit one's
complement doubling a negative word produces the end-around carry that
sets bit 1; doubling a positive one does not. So `CAE RNRAD / DOUBLE /
MASK BIT1` is exactly **"is RNRAD negative"**, and the high half is
`(RNRAD & POSMAX) + LVELBIAS` — magnitude only, 14 bits.

For the counts this project actually sends, that is correct:
`vel_raw_counts` returns `trunc(v/q) - LVELBIAS`, which the §13 window
gate now confines to `[0, 16383]`. RNRAD is therefore never negative on a
velocity read, the sign flag is always 0, and
`(RNRAD & POSMAX) + LVELBIAS` recovers `trunc(v/q)` exactly. **The sign
lead is dead**, and the window gate is what kills it — one of flight 19's
two "correct but not the bug" fixes turns out to close a different hole.

With §13a and §13b weakened and §13c closed, the TIG+360 s split has no
standing hypothesis. The bisection is now the right next move rather than
the fallback: **LR velocity data-good OFF entirely, altitude only.** If
TIG+360 survives, the defect is in the velocity chain and the altitude
chain is sound; if it does not, it is in neither and the search moves to
what the rope does with the reading (`SERVICER`'s reasonableness test and
`PSTHIBIT`, which is skipped before HIGATE).
