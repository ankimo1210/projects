# §26.13 Asian Options：M5bの受入条件と検証証跡

**判定：検証済み・独立レビュー待ち（pending_validation）。** acceptedへは変更していない。
M5bはA01–A06の実装・教材6小節・共有4図・配布物を対象とする。
原典：Hull 11e Global Edition §26.13、物理・印刷pp.626–627。日付：2026-09-16。
開始コミット：cb301c33。
[M5aの要求・独立検証](SECTION_26_13_REVIEW_2026-09-16.md)、
[vol10 notebook](../volumes/10_exotics_martingales/exotics.ipynb) §4.3.1–4.3.6、
[portal](../report/site/exotics.html#fig-asian_payoff)。

## 要求と証拠

| ID | 学習要求 | 本文・実装・検査 |
|---|---|---|
| A01 | 3契約の給付、観測日の規約（今日を除き満期を含む）、単位 | §4.3.1、同一経路で平均・満期値・行使価格を並べる図、観測日引数を持つ`asian_moments` |
| A02 | モーメント整合（式26.3・26.4＋Black）とExample 26.3の全表示値 | §4.3.2、$M_1$=52.59・$M_2$=2,922.76・23.54%・5.62を再現し、put-callパリティ残差を同じセルで表示 |
| A03 | 離散観測の$M_1,M_2$と12/52/250観測の6.00・5.70・5.63 | §4.3.4、O(m)の離散モーメント、観測数と価格の図、幾何平均の下界 |
| A04 | 既発契約の$K^*$変換と$K^*<0$のフォワード評価 | §4.3.5、恒等式の導出と4条件の表（$\bar S$=180で$K^*=-20$、確実に行使） |
| A05 | 平均行使型の給付と交換オプションとしての評価 | §4.3.6、`asian_average_strike`（Margrabe）と共分散の選択の明示 |
| A06 | 近似であること、誤差の向きと大きさ、適用域 | §4.3.6の実測誤差表と誤差図。144行で+23.35%〜−6.85%、Example 26.3自体が+0.99% |

## 契約と計算条件

$S_0,K$と価格は通貨、$T$は年、$r,q$は連続複利年率、$\sigma$は年率ボラティリティ。
リスク中立GBM、定数パラメータ、正の$S_0,T,\sigma$。観測日は$t_i=iT/m$で、
**今日を含まず満期を含む**。この規約でのみ原典の12/52/250観測の印刷値が再現する。

平均価格型の給付は$\max(A-K,0)$と$\max(K-A,0)$、平均行使型は$\max(S_T-A,0)$と$\max(A-S_T,0)$。
モーメントは厳密に

$$
M_1=\frac1m\sum_i F_{t_i},\qquad
M_2=\frac1{m^2}\sum_i\sum_j F_{t_i}F_{t_j}e^{\sigma^2\min(t_i,t_j)},\qquad F_u=S_0e^{(r-q)u}
$$

で、$F_0=M_1$、$\sigma_A^2=\ln(M_2/M_1^2)/T$をBlackに入れる。
離散版は$r-q$で割らないので$r=q$でもそのまま使える。連続版は可除特異点を級数で処理する。
既発契約は$K^*=\frac{t_1+t_2}{t_2}K-\frac{t_1}{t_2}\bar S$として新規契約を評価し$t_2/(t_1+t_2)$倍する。
これは近似ではなく給付の恒等式で、$K^*<0$ならcallは必ず行使されるためフォワードとして評価する。

## 図の意味と独立検証

4図は全て保存済み[lesson-data.json](validation/section-26-13/lesson-data.json)から作り、
`exotics.py`・両生成スクリプト・[prices.json](validation/section-26-13/prices.json)・
[numerical-check.json](validation/section-26-13/numerical-check.json)のハッシュが古ければ読み込みを拒否する。

| 図 | 内容 | 独立根拠 |
|---|---|---|
| `asian_payoff` | 定義した9点の折れ線、平均・満期値・行使価格と5つの給付 | テスト側で経路から平均と給付を再計算 |
| `asian_distribution` | 3市場の実測分布とモーメント整合した対数正規 | 二重ループの厳密モーメントと、当てはめ密度の再計算 |
| `asian_observations` | 6市場の12/52/250と連続値 | M5aの凍結表、および求積で独立に計算した連続価格 |
| `asian_error` | 6市場×3マネーネスの相対誤差 | M5aの凍結表（制御変量モンテカルロ、±4標準誤差） |

経路図はGBM標本ではなく定義した例である。分布図は20万パス・固定シードの標本で、密度の厳密解ではない。
**実測の歪度は当てはめた対数正規より大きい**（低ボラ短期0.099対0.088、正キャリー0.420対0.356、
高ボラ長期9.543対6.721）。当初「実測の方が小さい」と書いていたが3市場とも逆であり、修正した。

誤差の向きも同様に測り直した。観測52日では、putは$S/K$が大きいほど高く出て、
callは$\sigma\sqrt T$の小さい市場で$K$が平均より上のとき安く出る（ゼロキャリー$S/K=0.8$で−2.89%）。
「$M_1$より上では高く、下では安く」という片側の記述は測定と合わないため教材・図・portalから削除した。

## 生成・回帰・レビュー

[notebook記録](validation/section-26-13/notebook-m5b-check.json)は別実行との出力比較、
保存4図とビルダー出力の完全一致、§4.3以外の70セルが開始コミットと同一であることを確認する。
[ブラウザ記録](validation/section-26-13/browser-m5b-check.json)はChromium 141.0.7390.37で
Book/portal両面・1440/1000px・全13メニュー状態を検査し、18枚を保存した。
改変検出として表示中の誤差点を1ポイント動かし、数値検査が拒否することを確認している。

共有ソース（`exotics.py`・`figures.py`・`style.css`・notebook）の変更が及ぶ
§26.9・§26.10・§26.11・§26.12は、テストとブラウザ検査を再実行して新しい
`m5b-recheck.json`に記録した。過去の日時付き記録は上書きせず保存している。
再描画した画像は寸法が同じで、42枚中6枚が文字のアンチエイリアスだけ最大19/255・0.9%以下の画素で
異なったため、コミット済み画像を復元した。

## 受入の限界

モーメント整合は近似であり、この節の受入は**誤差を測って明示したこと**の受入であって、
価格の正しさの承認ではない。参照価格は標準誤差を持ち、厳密なのは2観測日の行だけである。
連続平均の参照値は$1/m$のリチャードソン外挿で、極限計算ではない。
平均行使型は幾何平均に対して厳密な共分散を使う近似で、−1.83%〜+18.23%の誤差を測った。
変動ボラ・現金配当・不等間隔の観測日・$T=0$・$\sigma=0$・全入力域の数値安定性は対象外。
Book数式は既存MathJax CDNに依存する。portalの外部通信は遮断して確認した。
Chromiumの指定幅での検査であり、モバイル・他ブラウザ・全巻の画面受入ではない。

**独立レビューは未実施**。M5aで定めた受入条件8「独立レビューを通ってからacceptedへ変更する」を
満たしていないため、台帳の状態はpending_validationに留めている。

## 実施済みの確認

- 関連テスト：Asian 33＋88（2 skipped）＋137。§26.9は123、§26.10は92、§26.11は192、§26.12は215（6 skipped）。
- notebookは83セル、36コードセル中33セルに出力。§4.3以外の70セルは開始コミットと同一。
- portalは98図、Bookはnotebook由来のページを生成。図は4つとも全幅カードに配置した。
- Chromium 141.0.7390.37でBook/portalを1440・1000px幅で確認。JavaScriptエラーなし。portalの外部HTTP(S)は遮断。
- 実画面の欠陥を3件検出して修正した：portalで図が半幅になり注記が枠外へ出ていたこと、
  Bookの狭い段でタイトルが切れていたこと、6市場のうち2市場が同色になっていたこと
  （portalテーマが`#2ca02c`を灰色へ再割当てするため、`#86868b`と衝突していた）。
- 保存画像を目視し、凡例・注記・タイトルの重なりがないことを確認した。
