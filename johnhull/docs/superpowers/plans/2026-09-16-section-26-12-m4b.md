# Section 26.12 M4b Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Read the assigned task brief, not the entire plan. Each task has a separate independent review.

**Goal:** Complete S01–S06 with an independent tree pricer, convergence evidence, shared teaching figures, and verified Book/portal delivery.

**Architecture:** A private torch-free CRR tree implements the optimal single-shout decision. Offline generation stores numerical lesson data; a private Plotly module reads those artifacts. The notebook and portal consume the same figures. Controller owns generated notebook, acceptance, ledger and regression evidence.

**Tech Stack:** Existing NumPy/SciPy/Plotly/pytest, Jupyter Book, Node Playwright. No production dependencies or public API changes.

**Spec:** johnhull/docs/SECTION_26_12_REVIEW_2026-09-16.md, S01–S06 and its six M4b acceptance conditions. User explicitly approved implementation of that handoff.

## Global Constraints

- Tracked changes only under johnhull; preserve original M4a inputs and unrelated WSET changes. Worktree /home/kazumasa/.codex/worktrees/johnhull-binary-m2a, branch codex/johnhull-shout-m4b.
- Reference prices42 and M4a generator/test/records are historical inputs, not tuning targets to rewrite. No oracle imports in hullkit pricing code. No production pricing API changes or dependencies.
- Price all42 call/put rows; put is an explicitly labelled extension. Main teaching/figures focus on the original call. The continuous-time problem is approximated by increasingly fine tree decision dates, not exactly solved by a finite tree.
- Synthetic positive S,K,T,sigma; r,q continuous annual rates, T years, money in currency. r=q is supported. Cash dividends/nonconstant volatility/discrete-contract calendars/T=0/sigma=0/all-domain stability are outside acceptance. Reject invalid inputs clearly.
- Single shout or no shout. Give the literal Hull payoff reading and intrinsic-floor reading, with their agreement when shouted S>=K. Original example K50, shout60 pays max(S_T-50,10).
- No heavy solves, downloads, GPU detection or training during notebook/report builds; numerical curves come from saved lesson artifacts. Deterministic cell IDs and executed notebook outputs required.
- Preserve all existing notebook cell sources. Insert new shout section between lookback and Asian, labelled 4.2 Shout (§26.12); existing Asian heading4.2 may be renumbered4.3 only, with that exact one-line exception recorded. Do not alter existing pricing examples/checks.
- Follow existing report/Plotly style. Only shout cards receive full-width layout. Add4 figures: total94, exotics14, themes12.
- Python /home/kazumasa/projects/.venv/bin/python; set PYTHONPATH=<worktree>/johnhull/hullkit/src:<worktree>/johnhull/report. exec_command workdir C:\Users\Kazumasa; Linux commands through wsl.exe with escalation as needed.
- No worker subagents, merges or pushes. Commit only assigned files, report actual tests/commands/results. Stop after3 failed repairs on the same issue. Parent runs one full project suite after integration.

## Task 1: Private tree and numerical acceptance data

**Files:** create hullkit/src/hullkit/_shout.py, hullkit/tests/test_shout_tree.py, scripts/build_shout_lesson_data.py, docs/validation/section-26-12/tree-check.json and lesson-data.json; modify MODEL_INDEX.md only for private module entry.

**Interfaces:** _price(spot,strike,rate,dividend,sigma,expiry,kind="call",steps=1024) returns float. _tree with the same inputs returns a dictionary containing price, immediate, continuation and arrays of remaining times plus shout-boundary lower/upper brackets. A scalar root decision and boundary diagnostics must arise from actual backward induction. Worker can propose a smaller interface adjustment before committing if numerical evidence requires it.

- [ ] Write independent failing tests against saved42prices, immediate-shout anchors and direct payoff integration. Run the failing import test before implementation.
- [ ] Implement recombining CRR with dt=T/N, u=exp(sigma*sqrt(dt)), d=1/u, p=(exp((r-q)*dt)-d)/(u-d), discounted child continuation, and analytic shout value using independently coded ndtr expressions. Terminal payoff is European intrinsic; each node takes max(continuation,shout). Reject nonfinite/invalid parameters and p outside[0,1]; never clamp a bad transition.
- [ ] Independently support put, do not use European call-put swap symmetry. Test call/put signed immediate values and r=q, near expiry, negative rates, dividend-above-rate, long/highvol from all42rows. No promise of monotone pointwise convergence for CRR.
- [ ] Measure N=128,256,512,1024 and if needed2048/4096 against the immutable42rows. Record every residual and max/RMS errors. Start acceptance target max abs<=0.03currency at finest N and demonstrate aggregate refinement improvement; tighten/document measured tolerance if appropriate, never change references. Report a blocker if target cannot be attained with justified refinement.
- [ ] Boundary checks use independent engineB at selected remaining times for positive-carry, zero-carry and long-high-vol calls. Report genuine node brackets/grid error, exclude root/near-expiry nodes without a bracket, do not present an interpolated boundary as exact. Independent engine may be imported by generator/tests, never by hullkit pricer.
- [ ] Generate deterministic lesson-data.json: contract metadata, original payoff pins, small N=3 call decision-node data, all42prices plus tree/ref/European/current fixed-lookback where defined, complete convergence data, and selected call boundary arrays/reference values. Each data family has units/method/limitations and source hashes. Do not rewrite prices.json/numerical-check.json.
- [ ] Add tamper-sensitive tests that reject wrong drift/discount, swapped put symmetry or a nonoptimal always/never-shout algorithm where feasible. Verify honest finite-step prices/root decisions rather than only monotonic/bounds tests.
- [ ] Run focused tests, ruff check/format with --no-force-exclude where needed, self-review and commit only assigned files. Report the adopted N, observed errors, boundary resolution and exact data schema for Task2.

## Task 2: Four shared lesson figures

**Files:** create hullkit/src/hullkit/_shout_lesson.py and hullkit/tests/test_shout_lesson.py; add private module MODEL_INDEX entry. Consume Task1 saved lesson-data.json only for expensive solves.

**Interfaces:** _figures() returns ordered dict shout_payoff, shout_decision, shout_boundary, shout_comparison. layout.meta has section="26.12", figure=key, active scenario and units. Each trace has semantic meta.role and scenario/contract where needed.

- [ ] Test expected4keys, semantic trace values and all menu states before implementation.
- [ ] shout_payoff: original K50/shout60; terminal grid30..100 with50/60/75, European and locked call payoffs plus separate10cash/call-at60 legs. Required pins ST30->10,60->10,75->25. A second shout50 state shows ATM identity. Explain agreement condition S_shout>=K.
- [ ] shout_decision: actual N3 tree from Task1, annotate node spot/continuation/shout/value and selected action. Explain continuation as discounted child expectation, shout as future guarantee plus European option, and one remaining right. Labels readable without overlap.
- [ ] shout_boundary: remaining-time axis, tree bracket or approximation plus independent B boundary, with convergence residuals in second panel. Three market menu states positive-carry/zero-carry/long-high-vol. State resolution and do not join unsupported root intervals into an invented exact boundary.
- [ ] shout_comparison: European/shout/fixed-lookback call prices at S80/100/125, K100, for all7market menus; r=q lookback is absent/explicitly unsupported, not zero. Label synthetic market and residual. Percent premium claims only for actually measured cases, not a universal20–31% theorem.
- [ ] Semantic tests compare original literal payoff pins, saved independent reference prices, actual signed node algebra and B boundary targets; refuse stale source hashes. Include a negative control preserving a sum while changing a constituent where relevant.
- [ ] Self-review, focused tests/lint, commit assigned files and report exact trace/menu contract for Task3.

## Task 3: Teaching, portal and browser verification

**Files:** modify vol10 builder, report/report_builder/figures.py, report/assets/style.css, report/tests/test_report_build.py, release_manifest.json, README.md, report/README.md; create scripts/verify_shout_lesson_browser.cjs. Controller owns generated notebook and records/statusdocs.

- [ ] Registry test first: all4newkeys, total94/exotics14. Add shared figures to portal, shout-only full-width selectors.
- [ ] Insert six S01–S06 teaching subsections: contract and50/60example; payoff/shout-value decomposition; CRR recursion and N3nodes; boundary/convergence; European/lookback comparison conditions; domain/limits and comprehension checks. Put prices are independent extension checks, not original Hull coverage.
- [ ] Use shared4figures, each in its own show cell; read expensive data from saved JSON. Explain finite dates approximate continuous shout, CRR oscillation/residuals, empirical oracle disagreement not proven error bound.
- [ ] Implement real browser menu controls, visible trace/value checks and math/example/boundary text checks for Book/portal. Inspect both1440/1000 widths for all states, numeric values at stated width. Use independent pins from controller browser-reference.json, including prices/node data/B boundaries, not producer values copied as truth. Add a signed-decomposition corruption check, restore and recheck.
- [ ] Capture each4figure at bothwidths/surfaces plus Book formula/example2widths (18images). Record source/artifact hashes, pageerror events, actual network restrictions and actual validation scopes. Portal HTTP(S) blocked; Book existingMathJax allowed/recorded.
- [ ] Run focused registry tests/lint/nodecheck, coordinate rebuilt assets with controller for one actual browser run, self-review and commit assigned sourcefiles. Report exact recorded states/values and limitations.

## Controller integration

Baseline: preserve M4a10files byte-for-byte and verify142tests/6skips plus artifact-ledger PASS before new implementation. Keep original checkout user changes intact while working isolated.

Review Task1 and Task2 before dependents; review Task3 after delivery verification. Controller can independently review oracle methodology/source PDF and produce browser pins while worker implements, without touching assigned sourcefiles.

Generate vol10, save/fresh-execute compare output signatures and separate4newPlotlydata/layout. Preserve previous source blocks except the documented Asian heading renumber. Build Book/portal, run all4section browser verifiers on rebuilt artifacts, visually inspect new4figures/formula at1000px. Create fresh M4b regression evidence for3prioraccepted sections; never rehash old datedPASS records as new execution. Do not pin a mutable ledger-count fixture in pricing evidence.

Run appropriate numerical/semantic/ledger tests and one complete johnhull suite. Update current validation/roadmap/feedback/handoff/guide/vol10progress, new acceptance note and section ledger only after S01–S06 checks. Target accepted4/gaps0/unreviewed302. Broad independent final review before completion. Keep any newly found gaps explicit; no invented tests/browser or convergence claims.
