# Section 26.10 M2b Implementation Plan

> 実施結果は[受入ノート](../../SECTION_26_10_ACCEPTANCE_2026-09-15.md)と[統合記録](../../validation/section-26-10/m2b-check.json)を参照。この文書のチェックリストは開始時点の計画を保持する。

> **For agentic workers:** Use superpowers:subagent-driven-development for task-scoped implementation/review; the controller performs builds, browser checks, evidence integration and final review. Follow checkbox steps.

**Goal:** Close D01–D06 from the accepted next-step note and accept §26.10 only after teaching, plots, independent numerical checks and actual Book/portal checks pass.
**Architecture:** An internal hullkit lesson module supplies deterministic payoffs, sensitivity data and four Plotly figures. The existing notebook and portal consume those figures. Pricing APIs remain unchanged. Evidence is refreshed after real reruns, including affected §26.9 artifacts.
**Tech Stack:** Existing Python/NumPy/SciPy/Plotly, nbformat/Jupyter Book, pytest, existing Playwright/Chromium; no new dependencies.
**Spec:** [Source-grounded requirements and next steps](../../SECTION_26_10_REVIEW_2026-09-15.md)

## Global Constraints

- Scope is johnhull only. Preserve unrelated user changes.
- No new production dependencies or changes to existing public pricing APIs.
- Use Hull 11e Global Edition §26.10 pp.622–623; D05 is correction of existing supporting explanations, D06 is delivery verification.
- Default synthetic market: S0=100, K=100, r=.05, q=.02, sigma=.20, T=1, cash payout Q=100. Units: currency, years, continuously compounded annual rates, annual volatility.
- Payoff convention: call triggers at S_T >= K; put at S_T < K. State this is the chosen settlement convention; equality has zero probability under the positive-T/sigma continuous pricing model.
- Closed-form pricing domain stays S0,K,T,sigma>0. Do not add T=0 or sigma=0 behavior to public pricers.
- All figures must show assumptions, axis units and distinguish actual settlement from one-sided limits. No misleading diagonal connection across a jump.
- No acceptance from parity alone; test selected individual values against independent terminal-density integrals.
- Book may require its existing MathJax CDN. Portal must work with external network blocked; clearly record each.
- Existing §26.9 acceptance requires fresh evidence for any shared changed source/artifact; do not blindly refresh hashes.
- Same issue: stop after three unsuccessful repair attempts.
- User has authorized implementation and main push in the ongoing session. Controller alone commits/merges/pushes after review/verification.

### Task 1: Shared internal binary lesson figures and semantic tests

**Files:** Create hullkit/src/hullkit/_binary_lesson.py and hullkit/tests/test_binary_lesson.py; add one MODEL_INDEX.md row for the internal module (the existing guard inventories private modules too).
**Interfaces:** Internal helpers below, importable by notebook/portal. No public export in hullkit/__init__.py.

- `_payoffs(terminal, strike=100.0, payout=100.0)`: return dict with numpy arrays under cash_call, cash_put, asset_call, asset_put; exact chosen tie convention.
- `_figures(S0=100.0, K=100.0, r=.05, sigma=.20, T=1.0, q=.02, payout=100.0)`: return an insertion-ordered dict of go.Figure under binary_payoffs, binary_replication, binary_spreads, binary_delta.
- `_cash_delta(S, K, r, sigma, T, q=0.0, payout=100.0)`: internal vectorized positive-domain formula Q exp(-rT) phi(d2)/(S sigma sqrt(T)).
- Use additional private helpers only if needed. Every helper should have a short docstring.

Figure requirements:
1. binary_payoffs: four clearly labeled cash/asset call/put panels. Show the exact K boundary with filled/open markers and separate segments so the jump is not drawn as a continuous ramp. Include Q and K. Preserve actual payoff points in trace data so tests and browser can check S_T=80,100,120.
2. binary_replication: dropdown with exact labels "call" and "put"; initial call. Each selection shows signed cash leg, signed asset leg, reconstructed payoff and vanilla payoff. At least the three probe points 80,100,120 must be in the data. Label components and total unambiguously. Title must include selection. Cash leg pays K (not arbitrary payout Q); match the payoff identities at the tie.
3. binary_spreads: two panels comparing normalized centered call-spread payoff and normalized butterfly payoff, for widths h=1,5,15. Formula: [call(K-h/2)-call(K+h/2)]/h and [call(K-h)-2call(K)+call(K+h)]/h². Explain that centered spreads equal 1/2 at K, so convergence to a digital payoff is away from the discontinuity; butterfly is localized, its price limit is discounted density. These are payoff curves, not pricing-density curves.
4. binary_delta: cash-call delta vs S0 near K for T=1,30/365,1/365, finite positive T; explain approach to expiry near K. Include probe S0=100 in each trace.

Give each figure `layout.meta = {"section":"26.10","figure":<key>,...market inputs...}` for robust Book/browser identification. Use existing Plotly template/style conventions; legends/annotations cannot overlap charts at 860px card width. No HTML/JS remote dependencies in figures.

- [ ] Write semantic tests first and confirm RED for missing helpers.
- [ ] Implement and verify exact tie/complement/payoff replication (multiple K/Q); evaluate four contracts individually.
- [ ] Validate numerical derivative via finite differences of independently integrated discounted payoffs, not the same delta helper or CDF formula; cover dividend and short positive T. Use the existing test_binary_reference terminal-density methodology without production imports in the oracle.
- [ ] Check all four figure contracts/labels and replication dropdown states. Test plotted delta points, payoff points, normalized spread/butterfly widths with separate reference calculations; add a parity-preserving wrong-individual-value negative control where useful.
- [ ] Run focused tests and ruff/format, then self-review and write the report. Do not generate the notebook, build outputs or edit docs beyond the MODEL_INDEX row. Controller handles commits.



Browser trace contract (controller integration; append to Task 1):
- Actual payoff segments: trace.meta={"role":"payoff","contract":"cash_call"|"cash_put"|"asset_call"|"asset_put"}. Multiple segments per contract are allowed; actual S_T=80,100,120 must be represented. Open one-sided-limit markers must use a different role, never be mistaken for actual settlement.
- Replication traces: trace.meta={"role":"cash_leg"|"asset_leg"|"reconstructed"|"vanilla","kind":"call"|"put"}. Dropdown visibility/title identifies current kind.
- Spread/butterfly payoff traces: trace.meta={"role":"spread"|"butterfly","width":h}.
- Delta curves: trace.meta={"role":"cash_delta","T":expiry}.
- Trace display names remain natural Japanese; metadata is only for robust semantic tests/browser verification.

### Task 2: Correct teaching and connect the notebook/portal

**Files:** Modify volumes/10_exotics_martingales/build_exotics_notebook.py, report/report_builder/figures.py, report/assets/style.css, report/tests/test_report_build.py, release_manifest.json, README.md and report/README.md. Keep changes limited to the binary lesson and resulting figure counts.
**Consumes:** Task 1 internal _binary_lesson helpers and four figure keys.
**Produces:** Corrected section 2, rendered four shared figures in Book, four FigureSpec cards in portal. Controller regenerates outputs.

- [ ] Replace the three existing binary cells before §3 with section 2.1–2.6 and appropriate code cells: 2.1 four payoffs/convention, 2.2 GBM/four prices/table, 2.3 call/put replication, 2.4 settlement risk, 2.5 spreads/butterfly, 2.6 delta.
- [ ] Explain all four payoffs, q/r discount distinction, all four prices, N(d2) as risk-neutral event probability and N(d1) as terminal-asset weighting/stock-numeraire probability. Do not call N(d1) the same risk-neutral event probability. With q>0, a stock-numeraire explanation must use the dividend-reinvested total-return stock; a terminal-asset weighting explanation is sufficient.
- [ ] State GBM/European/constant-parameter assumptions, variable units, positive T/sigma price domain, chosen tie convention and why it does not alter these continuous-model prices.
- [ ] Show call and put decomposition at payoff and present-value levels. State cash Q=K in replication, keeping arbitrary Q separate.
- [ ] Explain thin-market settlement risk from the discontinuous payoff and importance of agreed settlement definitions, without introducing a manipulation trading recipe.
- [ ] Correct the digital/butterfly conflation: -dC/dK is a unit cash digital; d²C/dK² is discounted terminal density. Explain the centered-spread tie value and convergence away from K.
- [ ] Correct finite-positive-time delta versus expiry-limit behavior. Show the finite formula and use the shared maturity figure.
- [ ] Display each of the four shared figures in a separate code cell via .show(); display a 4-contract price table and call/put replication calculations. Preserve S_B,K_B,R_B,SIG_B,T_B variables used later by the existing notebook. Leave §3 and all later lesson content intact.
- [ ] Show the four default-market prices as six-decimal strings in a displayed table with contract row labels cash call, cash put, asset call, asset put; the browser independently checks each labeled row (not just number presence). Use section heading text containing バイナリ and 26.10, and h3 subsections numbered 2.1 through 2.6.
- [ ] Register four portal FigureSpec entries with IDs equal to the four figure keys, clear Japanese titles/blurbs/practice comments carrying the assumptions and the relevant learning point. Reuse the same figure builders; no separate financial formulas in the portal.
- [ ] Give only the four binary cards a full grid row using .fig-card:has([id^="fig-binary_"]) { grid-column: 1 / -1; }, keeping existing card layout intact. Update portal contract counts from 82 to 86 and exotics from 2 to 6 in registry tests, manifest and current README summaries. Run focused registry tests with worktree PYTHONPATH.
- [ ] Check Python syntax and task diffs, then write report. Controller tests generated notebook and both delivery surfaces. No commits from worker.


Controller preflight: the unchanged final verification cell also consumes con, aon and van from the old binary cell, at q=0 and unit cash payout. Preserve these legacy aliases as well as S_B/K_B/R_B/SIG_B/T_B with their original values. Use distinct variables for the new lesson's q=.02,Q=100 price table/replication so the later barrier and assertion cells remain byte-for-byte unchanged and consistent.

## Controller integration and completion

- [ ] Prepare source-backed browser probes and independent figure/reference values; verify actual dropdown click changes selected content and check negative controls.
- [ ] Regenerate only vol10 and execute it to save outputs with existing verify_core_notebooks.write_outputs; compare against a separate fresh execution.
- [ ] Build portal and Book using the current worktree source paths.
- [ ] Browser: four binary figures on both surfaces; both replication selections; exact payoff tie; plotted delta probe values; math typeset/no math errors/no overflow; no JS errors; portal network blocked; screenshots at desktop and narrower card width.
- [ ] Re-run §26.9 independent/figure tests and its browser script after shared source/artifact changes; retain 8 choices on both surfaces and five static figures.
- [ ] Preserve the old M2a review as history; add a final acceptance note with D01–D06 proof paths, source/artifact hashes and limitations. Refresh the numerical/browser evidence that depends on changed files.
- [ ] Update ledger to accepted only after all requirements pass. Expected counts: accepted2, gaps_found0, unreviewed304.
- [ ] Run appropriate full project tests, ruff, release contract and both ledger modes. Review the whole change independently; fix material findings.
- [ ] Commit and reflect into main with scoped operations; verify final source equality/tests and push. No unrelated project changes.
