# Section 26.11 M3b Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox syntax. Read the task brief and global constraints; report full evidence in the task report.

**Goal:** Complete L01–L06 teaching, shared interactive figures, independent value checks and Book/portal delivery for Hull GE §26.11.

**Architecture:** Add one private, torch-free Plotly lesson module. The vol10 notebook and portal consume the same four figures. The existing production price API stays unchanged. Controller executes the generated notebook/builds, checks affected accepted sections, updates acceptance records and integrates main.

**Tech Stack:** Existing Python/NumPy/SciPy/Plotly, pytest, Jupyter Book, Node Playwright/Chromium; no installation.

**Execution note (2026-09-16):** The pre-existing Chromium cache was unavailable at delivery time. The installed Playwright CLI downloaded Chromium141 build1194; its stalled extraction was completed with system unzip into a task temporary directory. Browser checks used Chromium141.0.7390.37 at /tmp/johnhull-m3b-chromium.r6yWH2/chrome-linux/chrome. No repository dependencies changed. The runtime path below records the original plan, not the runtime actually used.

**Spec:** johnhull/docs/SECTION_26_11_REVIEW_2026-09-15.md, especially L01–L06 and the M3b acceptance steps; primary PDF physical pp.623–625.

## Global Constraints

- Scope all tracked changes to johnhull. Preserve user changes in other projects.
- Base commit: 4b828c61789b1fdcb5f96f10a1682ca787b0356d. Worktree: /home/kazumasa/.codex/worktrees/johnhull-binary-m2a, branch codex/johnhull-lookback-m3b.
- No production dependencies, public API changes or broad refactors. exotics.py and the existing abs(r-q)<1e-8 exception contract remain unchanged.
- L05 is implemented as explicit teaching of the valid domain and unsupported removable singularity. A new r=q pricing engine is outside this M3b, as allowed by the spec's step 3. Do not call the limit implemented.
- Keep builder text/cell sources before the lookback block and from the Asian block onward exactly unchanged. Keep lb = exotics.lookback_floating_call(S_B,S_B,R_B,SIG_B,T_B) for the later existing audit.
- New notebook code must not train, download, detect GPUs or add heavy simulation. Use deterministic examples and the existing price functions.
- All numeric inputs synthetic. S/K/history/prices in currency, T in years, r/q continuous annual rates, sigma annual. Continuous monitoring includes today/expiry.
- Use the existing Plotly/report style; only new lookback cards receive full-grid width. Do not rewrite report templates or shared Book configuration.
- Run Python with PYTHONPATH=<worktree>/johnhull/hullkit/src:<worktree>/johnhull/report using /home/kazumasa/projects/.venv/bin/python. The shared editable installation otherwise points to the original checkout.
- Windows exec_command workdir must be C:\Users\Kazumasa; invoke Linux commands through wsl.exe. Native paths are /home/... inside WSL.
- No worker spawns subagents or pushes/merges. Commit only assigned files after focused tests. Controller performs one full project suite after integration.
- Existing browser runtime: PLAYWRIGHT_MODULE=/home/kazumasa/.npm/_npx/e78b33305587cb7c/node_modules/playwright; CHROMIUM_BIN=/home/kazumasa/.cache/ms-playwright/chromium-1234/chrome-linux64/chrome; /usr/bin/node.
- Stop after three failed repair attempts on the same issue; report the blocker.

### Shared lesson contract

Create hullkit._lookback_lesson with private _path_payoffs(prices, strike=100.0, minimum_to_date=100.0, maximum_to_date=100.0, fixing_indices=None) and _figures().

_path_payoffs returns a dict with ordered keys floating_call, floating_put, fixed_call, fixed_put and scalar values. Include today and expiry in observations. The deterministic path below is defined by piecewise linear interpolation, not GBM or a Monte Carlo realization.

~~~python
times = np.linspace(0.0, 1.0, 9)
prices = np.array([100., 112., 94., 121., 90., 105., 84., 116., 108.])
keys = ("floating_call", "floating_put", "fixed_call", "fixed_put")
expected_new = [24., 13., 21., 16.]
expected_seasoned = [28., 22., 30., 20.]  # past minimum80, maximum130
expected_monitoring = {
    1: [8., 0., 8., 0.], 2: [18., 0., 8., 10.],
    4: [24., 0., 8., 16.], 8: [24., 13., 21., 16.],
}
~~~

_figures returns exactly lookback_payoffs, lookback_history, lookback_replication, lookback_monitoring. Each figure layout.meta has section="26.11", figure=<key> and relevant scenario/market parameters. Each trace has semantic meta for browser/test selection. Use only one updatemenu per figure; all states contain actual trace data and update visibility/layout. No server callbacks.

- lookback_payoffs: path + running min/max on left, four payoff bars on right. Menu labels "新規契約", "過去の極値あり", default new. Past histories (100,100)/(80,130), K100. Price/path trace roles "spot", "running_min", "running_max"; payoff bar role "payoffs", x=the four ordered contract keys. meta.scenario is "new"/"seasoned". Do not plot final payoffs as current prices.
- lookback_history: two panels, prices vs past minimum m0 on left (floating_call, fixed_put), vs past maximum M0 on right (floating_put, fixed_call). S100,r.05,q.02,sigma.2,T1. m0 grid includes 60..100 and 80/85/100; M0 grid includes 100..150 and100/120/130. Menu "K=80","K=100","K=125", default100. Each price trace meta.role="price", contract, strike. Current figure meta.K reflects selection. Required probes for all3 strikes: left80/85/100, right100/120/130. Explain history effects/flat segments, not an S0 curve.
- lookback_replication: price vs K grid60..140 including80/100/125; market above with m0=85,M0=120. Menu "fixed call","fixed put", default call. Five individually testable traces per selection: meta.role="floating_leg","stock_leg","cash_leg","reconstructed","fixed"; meta.kind="call"/"put". Call uses p_fl with M*=max(M0,K), +S exp(-qT), -K exp(-rT). Put uses c_fl with m*=min(m0,K), -S exp(-qT), +K exp(-rT). Display starred-history definitions, all units current currency; distinguish payoff identity from valuation. Never use reconstructed totals as the only numerical check.
- lookback_monitoring: full piecewise-linear path with fixing markers on left, four payoff bars on right. Menus "1区間","2区間","4区間","8区間", default8. Fixing indices range(0,9,8//n). Always today+expiry. roles "spot","fixings","payoffs"; meta.intervals=n. Histories100/100,K100. Full-grid8 intervals captures the extrema of this explicitly defined toy path exactly; no claim to continuous GBM convergence/price accuracy.

All subplot/axis/title/legend/annotation text must fit a plot container of 860px width; actual delivery checked at browser viewport1440/1000. Avoid legend/axis/annotation overlap and long unwrapped annotation strings.

### Task 1: Shared lookback lesson and independent semantic tests

**Files:** Create johnhull/hullkit/src/hullkit/_lookback_lesson.py and johnhull/hullkit/tests/test_lookback_lesson.py. Modify only the needed MODEL_INDEX.md row.
**Consumes:** Existing exotics.lookback_* APIs and M3a independent extrema-tail oracle in test_lookback_reference.py.
**Produces:** Exact private interfaces and trace/figure metadata in Shared lesson contract.

- [x] Write tests before implementation. Test fixed literal path payoffs/new+seasoned and all monitoring states; test raw curve/leg values against the M3a independent oracle, not another production pricer. Do not snapshot source text or cosmetic constants.
~~~python
def test_new_path_payoffs():
    from hullkit._lookback_lesson import _path_payoffs
    actual = _path_payoffs([100,112,94,121,90,105,84,116,108])
    assert list(actual.values()) == [24,13,21,16]
~~~
- [x] Record the failing focused test, implement the helpers and four figures using the exact shared contract, then run the focused semantic tests plus test_lookback_reference.py and test_model_index.py.
- [x] Cover every menu state, all curve points using the independent oracle where practical, adjusted histories on both sides of K, signed replication legs, and monotonic nested-fixing payoffs. Reuse the test-only M3a oracle without moving it into production.
- [x] Add only the internal-module inventory row needed by test_model_index.py. No public re-export or dependency.
- [x] Self-review the diff; run ruff check/format --check on changed Python. Commit assigned files and report tests/commit/concerns. Controller dispatches an independent task review before Task2.

### Task 2: Teaching, portal delivery and rendered verifier

**Files:** Modify johnhull/volumes/10_exotics_martingales/build_exotics_notebook.py (lookback block only), report/report_builder/figures.py, report/assets/style.css, report/tests/test_report_build.py, release_manifest.json, README.md and report/README.md. Create scripts/verify_lookback_lesson_browser.cjs. Controller owns generated exotics.ipynb, all acceptance/status records and other docs.
**Consumes:** Approved Task1 _figures() and metadata, and docs/validation/section-26-11/browser-reference.json (independently generated by controller).
**Produces:** Six teaching subheadings, four portal cards, 90 total figures/10 exotics, callable browser verifier which saves browser-m3b-check.json and evidence screenshots.

- [x] Extend existing report registry tests to require all4 lookback keys, total90/exotics10; run red before adding entries.
~~~python
expected = {"lookback_payoffs","lookback_history","lookback_replication","lookback_monitoring"}
assert expected <= {figure.id for figure in figures_for("exotics")}
~~~
- [x] Replace only the block between "# Cell 08: lookback md + demo" and "# Cell 09: asian md". Keep heading "### 4.1 ルックバック・オプション（§26.11）". Add six h4 headings4.1.1–4.1.6 for contracts/history, floating prices and example, history effects, fixed replication, observation frequency, scope/check questions.
- [x] Explain L01–L06 completely from PDF623–625: all4 payoffs, m0/M0 and mT/MT, positive S/T/sigma and valid extrema, continuous risk-neutral GBM, full floating formula coefficients a1/a2/a3/Y1 and b1/b2/b3/Y2 with units/discount/dividend roles, both Example26.2 prices, starred histories and both fixed identities, scoped vanilla inequalities, discrete vs continuous toy path, unsupported abs(r-q)<1e-8 removable singularity.
- [x] For Example26.2 display rows labelled "floating call"/"floating put", numeric prices to six decimals (8.037120/7.790219), original rounded8.04/7.79 and exact parameters S=m0=M0=50,r.10,q0,sigma.40,T.25.
~~~python
from hullkit._lookback_lesson import _figures as lookback_lesson_figures
lookback_figures = lookback_lesson_figures()
lb = exotics.lookback_floating_call(S_B, S_B, R_B, SIG_B, T_B)
~~~
Each of the four figure.show() calls goes in its own notebook cell at the appropriate explanation. Do not modify the global imports or cells outside this block. Prefix and Asian onward must remain byte/source identical.
- [x] Add FigureSpec entries consuming the same shared figures, with Japanese hints/practice explanations that distinguish path payoffs/current prices and contain the relevant assumptions/unsupported boundary. Set total figures90/exotics10 in manifest/registry tests/readmes. Add a lookback-only full-grid CSS selector alongside the existing binary selector.
- [x] Implement the browser verifier following verify_binary_lesson_browser.cjs mechanics, without changing that existing verifier. Root paths from __dirname, existing browser only; optional timeout via CHECK_TIMEOUT_MS.
- [x] Verify both portal/site exotics.html and Book notebooks/10_exotics.html. Match plots by unique layout.meta.figure. Click real dropdown controls and inspect visible traces for all states. Compare path/monitoring literal bars and exact fixing points, history prices and each replication leg at required probes against browser-reference.json. Compare Book example prices to contract-labelled rows.
- [x] Verify the actual running-min/max trace interpolation at the48 between-knot probes in browser-reference.json (24new/24seasoned). Task1 review identified node-only linear joining as incorrect for the piecewise-linear spot path; corrected extrema traces include crossing breakpoints.
- [x] Inject temporary +1 floating leg / -1 stock leg changes in replication while keeping reconstructed/fixed totals constant; require individual-leg validation rejection on each delivery surface, restore, and recheck before screenshots. This mutation is a test-only in-browser probe, never a source edit.
- [x] At widths1440/1000 check horizontal overflow, Plotly width, SVG text bounds, axis/legend and legend/annotation collisions, math overflow; errors0. Portal HTTP(S) blocked/requests0, Book existing MathJax CDN allowed and recorded. Confirm six h4 headings and boundary notice; wait for math typesetting.
- [x] Save screenshots for four figures at each width/surface and Book example table/section at both widths (18PNG). Follow existing settled-layout/sticky-header capture helpers. Controller builds delivery artifacts after this task is ready; coordinate a single real browser run before reporting DONE.
- [x] Run registry/render tests, ruff on changed Python, node --check on new verifier; self-review and commit only assigned source files. Report requirements/tests/commit/concerns.

## Controller integration and acceptance

The following are orchestration/verification of the two implementations, not additional feature tasks.

- Regenerate/save vol10 outputs and compare fresh output signatures (types/MIME/normalized deterministic text only). Separately compare saved4Plotly data/layout to shared builder. Confirm all cell sources outside lookback unchanged.
- Build portal90/12 and Jupyter Book31 pages; record warnings accurately. Run new lookback verifier and existing binary/barrier verifiers against rebuilt artifacts. Inspect all4 lookback screenshots and formula/example area at1000px.
- Run affected independent tests, ledger tests and one full johnhull suite. No rerunning all suites for metadata-only updates.
- Keep M3a numerical prices and old dated reviews as history. Create M3b acceptance/record and fresh regression records for §26.9/26.10 because shared builder/notebook/registry sources changed; bind current sources/artifacts only after actual checks. Do not claim new browser validation from hash changes alone.
- Mark L01–L06 accepted only with explanation/implementation-or-justified-NA/independent verification/figure/render evidence. L05 engine work is NA with the explicit scope reason above; unsupported r=q is a documented limitation.
- Update current ROADMAP/VALIDATION/feedback/handoff/guide/vol10 progress and generated ledger; expected counts accepted3,gaps0,unreviewed303.
- Independently review Task2 and final full branch; resolve required findings, then verify/commit remaining records, strict --require-tracked gate, ff main preserving unrelated changes, reflect ignored built assets, postmerge gates, push and verify remote.
