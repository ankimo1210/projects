# §26.16 M8 独立レビュー：指摘と対応

日付：2026-09-25。対象：`claude/johnhull-vol26-m8` の `7d04851e..dd2f63eb`（基点 `f6a2b62e`）。
観点の違う 2 つの独立レビューを並行して行った。A は原典照合・数値・教材本文、
B は検証の仕組みが本当に失敗しうるかと、記録・台帳の整合である。どちらもリポジトリを変更していない。
要求と納品時の記録は [REVIEW](SECTION_26_16_REVIEW_2026-09-25.md)、受入の判断は
[ACCEPTANCE](SECTION_26_16_ACCEPTANCE_2026-09-25.md) にある。以下の行番号は対象コミット時点。

## 判定の要約

価格式・原典の数値・独立参照の中核に誤りはなかった。A は Heston（Lewis）価格を
Fang–Oosterlee のベンチマークと自作の Gil-Pelaez（mpmath）で、CIR ラプラス変換の $E\sqrt V$ を
書き直す前の債券公式（40 桁）で、$\operatorname{var}(V)$ を 2 次キュムラントで照合し、いずれも機械精度で一致した。
受入を止める指摘は 1 件（F1、A は P2・B は P1）で、残りは P3 である。すべて対応した。

## 指摘と対応

| ID | 重大度 | 指摘 | 対応 |
|---|---|---|---|
| F1 | P1/P2 | VS05（VIX）の可視化の根拠画像 `book-volswap-formula-example-*.png` は §4.6.4 しか写しておらず、式 26.10 と累積分散が写っていない | ブラウザー検査に §4.6.5 の撮影（`book-vix-example-{1440,1000}.png`）を追加し、画面内に VIX の数値があることも確認する。台帳の VS05 visualization を新画像に差し替えた |
| F2 | P3 | vol 26 `redemption_only_principal_floor` が、2 つのクーポン配列を同時に 0 や 3 倍にしても通る | 契約のクーポン率を `jgbi_coupon_rate` として保存し、クーポン／指数比が face × rate / 2 に一致することを要求。0 倍・3 倍・率の改竄で落ちる改竄テストを追加 |
| F3 | P3 | vol 26 `nominal_payment_forward_measure` が `yoy_payment` の形しか見ず、0 や飛び値・2 行への切り詰めでも通る（今回の変更以前からの穴） | 支払年が 1..n の連続年で n ≥ 3、各期の始点フォワードが前期の終点フォワードと等しいことを要求。3 つの改竄テストを追加 |
| F4 | P3 | redemption-only と `floor_payoff_decomposition` が最終指数比・満期元本・額面を共有しているのに、依存が宣言されていない | 最終指数比の改竄ケースを追加し、`DEPENDENT_FAILURES` に依存を宣言 |
| F5 | P3 | 「Heston の相殺ノイズの $E(V)$ への影響は 1e-15 未満」は実測と合わない（約 2e-14） | 負になった価格の重みの合計を `strip_negative_q_max_weight`（2.40e-14）として記録し、記述を直した |
| F6 | P3 | `test_generated_reference_is_byte_reproducible` がメモリ上の 2 回の生成を比べるだけで、コミット済みファイルと比べていない | テストをコミット済み 2 ファイルとのバイト比較に置き換えた（`--check` と同じ内容） |
| F7 | P3 | 図が求積・MC を使わないことを確かめるテストが、lesson モジュールから届かない関数を差し替えていてほぼ空振り。実現分散の MC は 2 つの分母で同じ乱数を使い、実質 1 つの検査 | lesson モジュールが数値モジュールを持たないこと、`import hullkit` 後に図を作っても plotly 以外を読み込まないこと（別プロセス）を検査するテストに置き換えた。MC は分母ごとに別の乱数にした（−1.13 / +0.18 SE） |
| F8 | P3 | ブラウザー検査の MathJax 閾値 `>= 25` が実測 105 に対して緩い。状態数 32 がコンソール出力に直書き | 閾値を 100 に上げ（現在 112）、状態数は記録から数えて出力・保存する |
| F9 | P3 | notebook 検査が §4.6 の外をソースでしか比べておらず、26.9 の PNG・explorer や 26.15 の HTML の改変を通す。M7 にあった数値改変の拒否記録が無い | §4.6 の外の 101 セルを出力 53 件まで比較（実行番号は除き、Plotly の div UUID は伏せる）。出力の 1 文字改変、§26.16 図の数値改変、§26.15 図の数値改変の 3 つを拒否することを記録に残す |
| F10 | P3 | §26.15 BSK06 の locator「数値改変拒否」を refs が裏付けていない。制限事項「既受入6節の現行証跡はM7再検証記録へ」が古い | refs に basket の否定対照を含む `browser_run` を追加し、古い制限事項を削除した。M7 の Book ビルド記録への参照は外れたが、M8 のブラウザー再検査が最終 Book を開いて確認している |
| F11 | P3 | 本文 4.6.1 と `variance_notional` の docstring で、差が 0 以上になる前提 $V_K=\sigma_K^2$（と $L_{\rm vol}>0$）が抜けている | 本文・docstring・給付図の注記に前提を明記した |
| F12 | P3 | 本文 4.6.2「$1/K^2$ で割ると年率分散」は次元が不正確 | $e^{rT}p\,dK/K^2$ は無次元の累積分散で、$2/T$ を掛けて年率になると直した |
| F13 | P3 | 「歪んだスマイルを持つ Heston 3市場」の 1 つは $\rho=0$ | 「2 つは $\rho<0$、1 つは $\rho=0$」と直した |
| F14 | P3 | 「分散の分散はオプション価格の束からは決まらない」は言い過ぎ（相関ゼロなどの仮定の下ではボラ・スワップを複製する結果がある） | 「満期 1 本の束だけからは追加の仮定なしに決まらない」と本文・portal の説明を直した |
| F15 | P3 | 「誤差は ξ⁴ の速さ」「常に下側」は断定が強い。実測の傾きは 4.1–4.7、符号は分布の歪みに依存 | 「小さい ξ で ξ⁴ に比例（隣接点の傾き 4.1–4.7）、この市場ではすべて下側、一般の性質ではない」とし、傾きと符号は保存データから図に出す |
| F16 | P3 | replication 図の注記「差は最大 1.5e-12」は 3 市場の最大で、図の市場では 2.9e-15。レビューノートは差を求積誤差推定と並べていた | 図の市場の差と 3 市場の最大を分けて表示し、ノートの並べ方を直した |
| F17 | P3 | 「2004年以降の VIX」は原典どおりだが、CBOE の方式変更の日付は未確認 | 「原典によれば」と出典を明示した。CBOE の一次資料での確認はしていない |
| F18 | P3 | `numerical-check.json` の scope が「teaching and figures pending」、`milestone_status: gaps_found` のまま | 節の状態は台帳が持つとし、scope を参照記録としての内容に直して milestone_status を外した |
| F19 | P3 | 図の層に直書きがある（0 線の x 範囲、κ=2、strip の和の再計算、relative 状態の y 軸名に % が無い）。例題の T=0.25 が lesson builder と oracle の両方に直書き。oracle の docstring が再計算の範囲を誇張。README とポータル README が §26.16 を反映していない | 値はすべて保存データから取り、y 軸名は状態ごとに切り替える。T は reference.json に持たせ両者がそこから読む。docstring と README 2 本を直した |

## 変えなかったもの

- B が指摘したブラウザー検査の :95・:97・:113・:118 は、oracle 自体の健全性確認（画面の値は同じ状態で `arrayClose` が照合）なので残した。
- ROADMAP の「portalは102図」は §26.12 時点の履歴の記述として残した。

## 修正の再レビュー（`cc8e0bf4`）

修正範囲に絞った独立の再レビューを行った。P1/P2 と退行は無し。F1–F19 のうち 17 件は確認済み、
F3 と F7 は不十分だった。新しい指摘を含め、次の 6 件（すべて P3）を直した。

| ID | 指摘 | 対応 |
|---|---|---|
| N1 | §4.6.5 の「画面内に数値がある」確認は `textContent` を見ており、出力を折りたたんでも見出しとコードの文字列だけで通る | §4.6.5 の中で数値を含む `pre` が 1 つだけ表示され（高さ > 0）、撮影範囲の内側にあり、その数値が oracle と一致することを要求する。出力を隠した Book では、この判定が「表示されていない」になることを確かめた |
| N2 | YoY 配列を 3 行や 4 行に切り詰めても通る（F3 の残り） | `yoy_convexity_bp` を配列の $10^4\max\lvert jy-det\rvert$ と照合する。3 行・4 行への切り詰めと metric の改竄で落ちる改竄テストを追加 |
| N3 | 図の関数の中で numpy・scipy を import して計算しても、新しい 2 テストは通る（`import hullkit` の時点で読み込み済みのため。F7 の残り） | lesson モジュールのソースを AST で調べ、関数内も含めた import が hashlib・json・pathlib・plotly だけであること、`__import__`・`exec`・`eval` を呼ばないことを検査する |
| N4 | 統合記録の関連テスト件数が 488 / 13 のまま（実際は 489 / 14） | 記録を作り直した |
| N5 | REVIEW と FEEDBACK が、まだ無い ACCEPTANCE にリンクしている | 受入ノートを作成し、FEEDBACK と合わせて台帳の証拠に登録した |
| N6 | notebook の VIX セルに T=0.25 の直書きが残る | `T_EX` を使う |

あわせて notebook 検査で出力の stream 名も比べるようにした（stdout と stderr の入れ替えを検出する）。
再レビューが範囲外として挙げた「vol 26 notebook を作り直すたびにセル id が変わる」は今回の変更より前からの挙動で、直していない。

## 再検証

修正（N1–N6 を含む）の後に次を回した。lesson data・ブラウザー用参照・notebook（再実行）・portal・Book を作り直し、
notebook 検査と §26.16 のブラウザー検査（32 状態・20 画像）がともに PASS。
共有ファイルが再び変わったため、既受入 §26.9–§26.15 のテスト（1,232 passed / 6 skipped）と
ブラウザー再検査も最終ビルドで回し直し、すべて PASS。vol 26 は reference を再生成し（配列は不変、
metrics に `jgbi_coupon_rate` を追加）、SHA 契約に従い vol 21 も再生成した。
全体の件数とゲートの結果は [統合記録](validation/section-26-16/m8-check.json) にある。
