# M4b §26.12 independent final branch review

判定: **Approved**。重大度 P0–P2 の指摘なし。検出した非ブロッキングの P3 は、本文1文を実装へ合わせた `8b526e42` でソース上解消した。生成物への反映と証跡更新は controller の統合確認に残る。

## 対象と方法

- 広範囲レビュー対象: `134e1c16..bb1636b3`。M4a入力のチェックポイントは `98e719a6`。主実装差分 `98e719a6..bb1636b3` のファイル索引とソース、要求表S01–S06、実装計画、現在の受入ノート、台帳と統合記録を確認した。
- M4aの参照価格・生成器・試験・当時の要求文書は凍結入力として扱った。全体3MB差分や巨大JSONは展開せず、ソースの範囲指定とJSON構造の直接検査を使用した。
- Task1–3の独立レビューを読み、数値計算・共有図・配布・証跡の接続を追加検査した。既存スイートとブラウザー検査は再実行せず、後述の限定的な独立プローブだけを実行した。
- 本レビューはソース編集・コミットを行っていない。保存画像の目視用一時コピーとこのレビュー記録のみ作成した。

## 指摘

### P3 — 同値時の選択について本文と価格器の説明が不一致（ソース修正済み）

対象: `johnhull/volumes/10_exotics_martingales/build_exotics_notebook.py:854`。

`bb1636b3` の本文は「同値なら継続します」だったが、`johnhull/hullkit/src/hullkit/_shout.py:93,116,123` は `immediate >= continuation` で宣言を選択する。価格は同じでも、教材の手順と診断ラベルの説明が一致しない。価格精度・既存例・受入の主要条件を損なわないため P3。

controllerへ共有後、`8b526e42` の差分が本文1文のみであることを確認した。「同値なら宣言を選びます（価格は同じです）」は実装と一致し、この指摘はソース上解消。価格器を変更する必要はない。修正後のnotebook/Book/portalと証跡の鮮度は、controllerが更新後の限定確認で閉じる。

## 横断的な確認結果

| 領域 | 確認と根拠 |
|---|---|
| 契約・価格器 | `_shout.py:17–27,54–68,89–123` のcall/put別の符号付き現金脚、ATMオプション、r−qの遷移とrの割引、満期intrinsic、後退maxが整合。oracle importと公開API変更なし。 |
| 数値独立性 | `build_shout_browser_reference.py:29–42,45–68,71–125` は新価格器・図を呼ばず、密度積分、凍結価格、極値積分、別の小木再帰、独立B境界を使う。`test_shout_tree.py:125–182` は保存残差と境界を実計算し直す構成。 |
| 収束・境界の限界 | `build_shout_lesson_data.py:67–138,150–219` と受入ノートのN1024最大絶対差0.003792916424、許容差0.005が一致。15境界チェック中1点の非包含と、高ボラ時の粗い節点幅を図・本文で明示し、厳密包含や一般誤差上限へ読み替えていない。 |
| artifact-only・鮮度 | `_shout_lesson.py:40–56,374–382` は必須6ソースを各familyで検証してから保存値を読む。必須ハッシュ欠落36ケースも保持。図構築に価格探索を混入させていない。 |
| S01–S06 | builder `800–819` が契約、`830–838` が分解、`842–875` がCRRと境界/収束、`883–897` が比較、`905–919` が適用域と理解確認。台帳のS04→4.2.5、S05→4.2.6、S06→4図の対応が正しい。 |
| 独立比較の範囲 | 現行本文・受入ノートは42価格、ATM14行の二方式照合、lookback上界36行/6市場を区別。r=qのlookback欠測とput拡張も明示。M4a履歴の広い主張を現行の結論へ流用していない。 |
| 実画面の検査 | `verify_shout_lesson_browser.cjs:35–101,126–149,151–212,214–240` は実メニュー、可視traceの数値、描画済み節点文字、符号付き脚の破損検出と復元、両幅の配置・数式・例を検査する。N1024以外の残差や境界格子検査の限界も明示。 |
| 配布・既存図への影響 | registryの4図追加とmanifestの94図が一致。CSSはshoutカードのIDに限定。過去3節の新規m4b再検証を現在の台帳へ接続し、履歴と区別している。 |

## レビュー担当が追加実行した確認

1. 独立の標準正規密度積分でreset optionを求め、再帰で後退評価した。4市場（OTM正carry、ITM負金利、r=q、長期高ボラ）×call/put×N=2/5、**216節点**が実装と一致。最大絶対差 **1.3642420526593924e-12**。既存の価格器・テストヘルパー・参照生成器を独立期待値の計算に使っていない。
2. r=qでcall/put、N=63、S/Kを0.01倍と10,000倍にした**4件の同次性**を確認。倍率を戻した最大差 **1.0658141036401503e-14**。
3. 新節と既存3節の現行m4b記録、lesson/tree/browser参照、台帳evidenceを再帰的に検査。`bb1636b3` 時点の **359ハッシュ照合で不一致0**。これは本文修正前のスナップショットに対する結果であり、修正後の再pinを代替しない。
4. `input-pins.json` の過去 **18数値・統合JSONは全て一致**。M4aの価格42行、数値記録、参照生成器、参照テスト、要求文書は `98e719a6` とバイト一致。旧lookback画像18枚も `98e719a6` と一致。
5. 旧notebookの **64セルの本文・種類を順序付き照合**。追加後74セルで、既存本文の差は許可済みのAsian見出し4.2→4.3のみ。保存済みShoutの4 Plotly payloadのdata/layoutは現共有builderと完全一致。
6. 全体pytest・関連pytest・Book・portalの**4ログのSHA256**は統合記録に一致。全体ログ末尾の **1916 passed / 6 skipped / 2 warnings / 86.05s** を確認した。既存のjapanize_matplotlib/distutils警告とr=qのlookback参照スキップを新shoutの欠落とは解釈していない。
7. ブラウザJSONで **Book/portal各26状態、計52状態**のnumeric_checked、両面negative controlの拒否・復元、layout/page errors 0を直接確認。portal外部要求0、BookのMathJax CDNとフォント8要求の記録を確認。
8. 保存画像 `book-shout_comparison-1000.png`、`portal-shout_boundary-1000.png`、`book-shout_decision-1000.png` を目視。凡例・注記・モードバーとタイトルの分離、負の現金脚、境界非包含の記載を読めた。これ以外の画像の目視やブラウザ再操作を本レビューで行ったとは主張しない。
9. `134e1c16..bb1636b3` の追跡差分はすべてjohnhull内。台帳を直接集計して **accepted4 / unreviewed302 / gaps0** と一致。新しい本番依存・公開価格API・他プロジェクトへの追跡差分なし。

## 統合時の確認範囲

- `final_review=PENDING` は本レビュー待ちの意図的な状態として扱い、欠陥とは判定しない。controllerがこの判定を記録し、台帳の保存ハッシュを更新する。
- `8b526e42` の修正後は生成物と現在の証跡を限定的に再確認する。数学実装は変更されておらず、既に成功した全体pytestの再実行を必要とする懸念はない。
- 有限Nの近似、境界の非包含と粗い幅、正入力の合成GBM範囲、Bookの既存CDN依存、記録されたブラウザ・画面幅の範囲という制約は残る。受入文書はこれらを明示している。

最終ブランチレビュー判定: **Approved**。


## Scoped re-review — P3修正後の生成物・証跡（2026-09-16）

最新判定: **Approved**。P3は生成物への反映まで含めて解消。レビュー上の未解決指摘なし。

対象は本文修正コミット `8b526e422771a21bb2abb27e8b20ec1c7b2394d3` と、その後controllerが再生成した未コミットのnotebook/Book/portal・検証記録。全体再走査、pytest、ブラウザ再操作は実施せず、依頼された本文反映・ハッシュ・過去成果物保存だけに限定した。

- builder、保存notebookのセル本文、生成Book HTMLの各1箇所に「同値なら宣言を選びます（価格は同じです）」がある。3箇所とも旧文「同値なら継続します」はない。
- 現行のsource/artifact/family/history/ledger evidenceを359件照合。最初は358件一致、§26.12のacceptance_noteだけ不一致だった。controllerが同時に受入ノート末尾へ判断・理由・変更影響の表を追記したためであり、再pin後に当該1件を限定再確認して一致した。他358件の再実行は行っていない。
- 再確認した受入ノートSHA256: `100f5d228bc15a4af43e919d71eb5819e07187ee270a1a60ccb316a72f64f735`。
- 確認時点の `m4b-check.json` SHA256: `84adb9ad9dbe5b27384fb6797c3b270fe372cbd65df90ec8eb165ab422039345`。`final_review=PENDING` は本判定の取り込み待ちとして確認した。
- 過去18数値・統合JSONはinput-pinsと全件一致。旧lookback画像18枚は `98e719a6` とバイト一致。
- 本文修正は診断上の同値時選択を正しく説明する変更であり、価格器・数値結果を変更しない。広範囲レビューの結論は維持する。

本追記で、上記「統合時の確認範囲」に残していた生成物・証跡の限定確認を完了した。controllerによるdurable copy、final_reviewフラグおよび対応する台帳ハッシュの最終更新は通常の統合処理として残る。
