# §26.13 M5b 独立レビュー：要修正

日付：2026-09-16。対象：`961b5478`（M5a `cb301c33`、開始点 `a96e0a42`）。
**判定：P2が6件未解決。受入不可、`pending_validation`を維持する。**
本ノートは独立レビューの結果であり、修正完了・再受入の記録ではない。
[要求](SECTION_26_13_REVIEW_2026-09-16.md)・[実装側の受入記録](SECTION_26_13_ACCEPTANCE_2026-09-16.md)・[ROADMAP](../ROADMAP.md)と併せて読む。
以下の行番号は対象コミット時点。価格式に明白な主要欠陥は見つからなかったが、説明の一般化と表示値の独立検証に不足がある。

## 未解決の指摘

### F1 — P2：連続平均の r=q を未定義と説明している

- 場所：`volumes/10_exotics_martingales/build_exotics_notebook.py:955–956`。
- 本文は連続平均のモーメントを未定義とし、離散観測を渡すよう案内する。しかし印刷式の分母は可除特異点で、現行APIは連続平均の極限を計算する。
- 再現：`asian_moments(100, .03, .30, 2, q=.03)` は `(100.0, 10628.001927043299)`、同条件・K=100の `asian_average_price` は `9.248799886494512`。
- 修正条件：印刷式への直接代入と、モーメント・実装自体の定義を区別する。本文、保存notebook、Bookの説明を揃える。

### F2 — P2：全表示点で標準誤差が十分小さいという注記が誤り

- 場所：`hullkit/src/hullkit/_asian_lesson.py:307`。Book/portal共通の誤差図。
- 「標準誤差は各点の誤差よりはるかに小さい」は表示29点中4点で成立しない。
- 保存データの `abs(TW-reference)/SE` は低ボラ短期call S/K=1.25とput S/K=.8で各0.314、負金利ATM putで0.385、callで0.545。負金利ATM callの差は−0.000089819、SEは0.000164847。
- 修正条件：大きな近似誤差とMC不確実性に埋もれる小差を区別し、全点で符号が確定したと説明しない。修正後の注記を両画面で確認する。

### F3 — P2：誤差レンジの集計対象を144行全体と表示している

- 場所：`docs/SECTION_26_13_ACCEPTANCE_2026-09-16.md:20`、ROADMAPのM5説明。旧M5aノートにも同様の記述がある。
- +23.35%〜−6.85%は144行中、**参照価格が0.5を超える119行**の集計。生成器の抽出条件は意図的だが、要約で抜けている。教材§4.3.6には閾値の注記があり、全面的な欠落ではないが件数を区別すべき。
- 反例：negative-rate、250観測、call、S/K=.8。TW=0.0110631050954、参照=0.0135400654585、SE=0.0000532925660、相対差−18.2936%（約46.5 SE）。
- 別シード20万パス（seed=719123）でも参照=0.0135978548815、SE=0.0000783675358、相対差−18.6408%を確認した。
- 修正条件：全144参照と、閾値で選んだ119行の誤差集計を明示的に分ける。旧記録の無断書換えではなく、現在の説明から訂正を参照できるようにする。

### F4 — P2：測定で反証された誤差方向の断定がAPIと生成器に残る

- 場所：`hullkit/src/hullkit/exotics.py:358–362`、`scripts/build_asian_reference.py:476`、`docs/validation/section-26-13/numerical-check.json` の `approximation_error.sign`。
- 本文から削除したとされる「平均のフォワード以上では高く、以下では安く」という一般化が残っている。
- 反例：zero-carry、52観測、S=80、K=100、r=q=.03、sigma=.30、T=2。M1=80、TW=2.1403151470、参照=2.2040820781、SE=0.00104919184。高い行使価格側のcallで−2.89%（約60.8 SE）。
- 修正条件：方向の一般則を削除するか、市場・契約・マネーネスを指定した観測結果へ限定する。docstringだけでなく生成器と現行証跡も整合させる。

### F5 — P2：既発契約の受入証拠と実表示が異なる条件を使う

- 場所：`docs/SECTION_26_13_ACCEPTANCE_2026-09-16.md:18`、notebook builderの `rows_seasoned`、`scripts/verify_asian_lesson_browser.cjs:261–265`。
- 受入記録とlesson-dataはK=100を用い、観測済み平均180でK*=-20。実notebookはExampleのK=50を引き継ぎ、同じ平均180でK*=-145を表示する。
- 実表示は平均80/100/120/180に対してK*=5/−25/−55/−145、価格17.6837/29.2132/40.7426/75.3311。別の有効な例ではあるが、受入記録が主張する例と一致しない。
- テスト側のseasonedデータは表示表に接続されていない。ブラウザ検査は「確実に行使」「恒等式」の文字列存在のみで、条件・K*・価格を数値照合しない。notebook再実行比較もこの食い違いを拒否できない。
- 修正条件：採用する例の入力条件を明示して教材・独立参照・受入記録を統一し、表示表の各数値を独立参照へ照合する検査を追加する。

### F6 — P2：観測数と価格・幾何平均の上下界を一般化しすぎている

- 場所：`volumes/10_exotics_martingales/build_exotics_notebook.py:989–992`。
- 「観測を増やすと平均価格オプションは安くなる」は一般則ではない。保存参照のnegative-rate、call、S/K=1.25は12→250観測で23.7262677411→23.8377781388と上昇する。平均の期待値も観測規約とcarryによって変わる。
- 「幾何平均価格は算術平均の下界」はcallに限定する。putでは逆に上界。positive-carry、52観測、ATM putでは幾何平均価格3.9204117920、算術平均参照3.7883747678（SE=.0002227711）。
- 修正条件：図・Exampleで扱う条件へ価格減少の説明を限定し、call/putの上下界を区別する。

### F7 — P3：満期1観測の平均行使型に微小な正価格

- 場所：`hullkit/src/hullkit/exotics.py:418–420`。
- `asian_average_strike(100, -.025, .18, .75, q=.01, times=[.75])` は `6.678668867331794e-7`。この契約はA=S_Tであり、給付は厳密に0。丸め誤差による微小値で、受入ブロッカーとはしない。
- 推奨：満期1観測を明示的に扱い、既存6市場でゼロ給付の恒等式を検査する。

## 確認した証拠と限界

- Hull 11e Global Editionの原典§26.13、物理・印刷pp.626–627を直接照合。
- 数値と配布検証を別の独立レビュー担当が調査。参照エンジンはhullkitを呼ばず、離散モーメント・条件付き求積・幾何平均制御変量MCを利用。
- 今回再実行：Asian関連 **258 passed / 2 skipped（1.52秒）**。スキップは印刷された連続式のr=q直接代入であり、APIが未対応という意味ではない。
- 同一HEADの直前の全体再実行：**2,180 passed / 8 skipped / 2 warnings（84.49秒）**。今回のレビュー中にソース変更はない。全体検査と今回のscoped検査を混同しない。
- 台帳 `--check-artifacts` PASS。§26.9–26.13のM5b関連source/artifactハッシュ218件一致。
- 保存4図のdata/layoutは現行builderと完全一致。§4.3外70セルのsource/orderは開始コミットと同一。notebookのメモリ内再実行と保存出力の比較もPASS。
- 独立browser-referenceと20万path分布の再計算が保存値と一致。lesson loaderの必須hash削除25通りはすべて拒否。
- 元証跡を保護する一時コピーで、ChromiumのBook/portal両面・1440/1000幅・全13状態（計52状態）を再検査しPASS、18画像を生成。保存Bookの狭幅画像で誤差表・観測数図・誤差図を目視した。
- これらのPASSはF1–F6の内容上の誤りを否定しない。ブラウザ検査が再現性・配置・指定された値を検査しても、本文の一般則や未接続の表は別に検証する必要がある。
- browser-referenceのhash検査は列挙されたキーのみで、必須キー欠損検査はない。現行証跡に欠損はなく、確定不具合には数えていない。
- 全入力域の数値安定性、極小ボラ領域など宣言済み対象外について受入を主張しない。

## 次の作業と受入条件

1. F1–F6を修正する。特にF5は説明だけでなく、実表示と独立値を結ぶ検査を追加する。
2. 影響する教材データ・notebook・Book/portalを再生成し、ソースhashと最新検証記録を更新する。過去の日時付き証跡は保存する。
3. 関連テストと実ブラウザ検査、共有ファイルの変更が及ぶ既存節の検査を実施する。
4. 修正後の独立再レビューでP2が解消したことを確認してからacceptedを検討する。
5. VALIDATION、vol10 PROGRESS、引継ぎノートのM4bで止まった現在地を整理する。日付付き履歴は現行状態と区別する。

本記録保存時点：受入4節・pending_validation 1節・未評価301項目。実装修正、台帳の受入変更、commit/pushは本ノート保存の範囲に含めない。
