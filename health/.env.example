# Google OAuth 2.0 client credentials (Google Cloud Console > APIs & Services > Credentials).
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Health Planet developer application credentials.
HEALTHPLANET_CLIENT_ID=
HEALTHPLANET_CLIENT_SECRET=

# Legacy Streamlit setting (CLI uses explicit sync --history-start instead).
HEALTH_BACKFILL_START=
