"""jp_llm_lab — educational Japanese LLM lab (visualization-first).

This package implements a small Japanese decoder-only Transformer from
scratch, with the explicit goal of making every internal mechanism —
tokenization, attention, residual streams, training dynamics, calibration,
generation — observable and understandable. It is NOT designed to produce a
competitive language model.
"""

__version__ = "0.1.0"
