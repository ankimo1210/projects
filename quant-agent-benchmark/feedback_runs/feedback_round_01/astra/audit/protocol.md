# feedback_round_01 / Astra — 事前プロトコル

開始 2026-09-05 07:54:25 UTC、上限60分。08:44:25 UTC以降は新しい設計を加えず、08:54:25 UTCまでに検証・保存する。完全版の受領前の準備と中断時間も含む。MODEL_KEY=astra は以前のユーザー明示割当で確認済み。実際のバックエンドのモデル・推論設定は独立確認できず、切替しない。ネットワーク・追加エージェント・他モデル・採点器は利用しない。

## 範囲と保全

指定の `feedback_runs/feedback_round_01/astra` のみへ書く。初回の79成果物を初回manifestからハッシュ化し、baseline_workspaceへコピー。仮想環境、Git、build、dist、egg-info、キャッシュは除く。完全版受領前に旧指示の許可範囲で作った `results/astra/improvement_round_1` はこの作業の準備コピーであり初回原本ではない。そこではコピーとメタデータ記録、コード閲覧だけを行い、実験・テスト・環境変更はしていない。以後利用も変更もしない。終了時に初回79ファイルの不変を検証する。

## 事実と仮説

確認事実：高度モデルは x=log(1+T) 上の自然3次ゼロ金利スプライン。15年以降の節点は15/20/25/30年。目的はデータの Huber 損失と nλ∫(d²z_bp/dx²)²dx。最終元係数 λ=0.001、閾値2.5。長端もx座標の自然境界。支払列は評価日起点の定期日と最後の端数日、最後の利息を実期間で按分。これが公開文書の唯一の解釈とは言えない。

仮説 H1：端数規約が残差の一部を説明する。H2：log時間の罰則の尺度が長端の実形状を抑える。H3：長端ノットの密度が不足する。H4：端点条件が長期の傾きを歪める。H5：Huber重みが正当な長端構造まで低下させる。いずれも未確認であり、誤差を見て正解と決めない。

## 順序と一要因実験

1. コピー上で元CLI・全テストを実行し、版、公開データハッシュ、元成果物との差を保存。
2. 独立した支払列・価格式に既知の D(T) を渡し、価格付けだけ検証。平坦、負金利、右上がり、山、長端の山を用い、整数／端数満期、単位を確認。按分・端数利息なし・全額の差は規約差として記録し、適合だけで規約を変更しない。
3. 価格付けを按分で固定。元高度モデルを比較元とし、(a) λを0.0001、0.01に変更、(b) 長端ノットを2.5年間隔／15,22.5,30に変更、(c) 最長端のz傾きを0へ、(d) x曲率の二乗重みをT>10だけ(10/T)^2へ、(e) Huber閾値1.5、4、頑健化なしを個別に比較する。各実験ではその他の値を固定する。単純Nelson–Siegelも同じ人工観測で測る。
4. 合格候補があれば単独の1変更を採用する。複数の数値変更を合成しない。JSON・HTMLの互換性修正は数値変更とは別IDで記録する。

## 分割と測定対象

公開データ：初回と同じ満期群S0を残し、同じ群の選択位置を変えるS1/S2と、T>=20を隠す長端S3を追加する。S1/S2は学習側に残る検証点近傍を0.03年(T<=2)、0.3年(2<T<15)、0.6年(T>=15)の幅で除く。除いた学習点はembargoとして記録する。難しい群を誤差を見て除外しない。公開単位補正は比較可能性のため初回の可視テープ全体の処理を固定し、残る前処理漏洩を明記する。

人工データ：公開商品の属性を観測配置に使うが、価格は独自の解析カーブと独立式で生成する。原本データは改変しない。平坦、負の短端から上昇、右下がり、中期の山、長期の山の5形状。各形状に通常ノイズ、10%観測除去、長端低流動性、少数異常値の4条件を固定seed=20260905で作る。中程度の異常値を含む価格摂動は再生成せず全候補で共有。真値は自作のみに限定する。

年限区分は短期T<=2、中期2<T<15、長期T>=15。商品はdeposit/ois_swap/bondを分け、空の区分はnull。人工ゼロ・フォワードRMSEはbp、公開の金利誤差はbp、債券誤差は価格ポイント。標準化残差は(model−quote)/max(半スプレッド,金利0.1bpまたは価格0.0125)、重み付きHuberは初回と同じ流動性倍率。生の異単位RMSEは合算しない。

## 採用条件（新しい結果を見る前に固定）

必須：独立価格式と一致（率の絶対差1e-10、価格1e-8）、単位／内部整合性／全最終テスト／クリーンCLI再現性に不合格がない。

数値変更は次をすべて満たす場合だけ採用する。

- 人工20条件平均の長期ゼロRMSEが元高度モデルから5%以上改善。
- 各人工条件・各年限のゼロRMSE悪化は max(元の10%, 0.25bp) 以下、フォワードRMSE悪化は max(元の10%, 1bp) 以下。
- 公開4分割の平均Huber損失の悪化は2%以内。各分割・年限・商品セルのRMSE悪化は max(元の10%, 金利0.25bp／債券0.025ポイント) 以下。
- 人工の真のフォワードからの最大乖離増加は max(元の10%, 2bp) 以下。単純な真の形状で新しい極端な振動を生まない。図と年限別重みも確認する。

合格候補は人工の長期ゼロRMSEが最も小さいものを選ぶ。同等なら変更が少ないもの。全候補不合格なら数値部分を初回のまま保存し、「確認できた採用可能な改善なし」と報告する。特定条件だけ良い結果を平均に隠さない。改善率=(前−後)/前。分母0、欠測、測定条件不一致ではnull。

## 計画変更の記録

完全版受領時：出力先・承認Pythonを指定どおり適用。初回準備の開始時刻は維持。上限60分は同一で延長なし。上記実験・採用条件は新しい数値結果を見る前に固定した。

2026-09-05 08:22 UTC以降の整理：数値候補はすべて不採用で確定。採用閾値・分割・入力は変更なし。規約分離の補足としてOIS頻度の別解釈を固定D上だけで測定（再校正・採用なし）。既定の人工通常条件の重みを診断のため再取得。図では端点条件が悪化した長期山・低流動性を明示。いずれも選択規則の変更ではない。
