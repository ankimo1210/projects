[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"
[project]
name = "quantcurve-flawed"
version = "0.0.1"
requires-python = ">=3.12,<3.13"
dependencies = ["numpy>=2,<3", "pandas>=2.2,<3", "scipy>=1.13,<2"]
[tool.setuptools.packages.find]
where = ["src"]
