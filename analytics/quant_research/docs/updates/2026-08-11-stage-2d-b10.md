# 2026-08-11 — Stage 2D / B10教材実装

## 結論

B10 Week 37–40とProjectの6 Notebookを実装した。座学教科書の実装範囲はB1–B10、
Week 1–40、60 Notebookとなり、最終target B1–B11 / 44週に対して40/44週、丸めて91%である。

B10は、一度動くB9 Notebookをproductionへ配備する章ではない。correctness、performance、
point-in-time data、experiment lineage、promotion・rollback境界を、第三者が再実行・監査できる
research packageへ変換する章である。B9 locked outerは未開封、registryのproduction pointerは
`None`のまま保持した。

## 実装内容

| Chapter | 内容 | 実行済みcode cells |
|---|---|---:|
| 54 | B10 evidence chain、failure surface、claim boundary | 4 |
| 55 | vectorization、reduction order、warm-up後median/IQR、chunk plan | 5 |
| 56 | package API、test portfolio、config・data・code hash | 6 |
| 57 | five-time contract、pandas/SQLite PIT、schema・column memory | 6 |
| 58 | immutable registry、PSI/KS drift、batch lineage、rollback | 7 |
| 59 | development-only B9 numeric/TF–IDF pipelineの再現 | 5 |

共通libraryとして次を追加した。

- `data_systems.py`: timezone-aware bitemporal record、pandas/SQLiteによる独立PIT snapshot、
  schema evolution、column-oriented memory audit
- `research_engineering.py`: benchmark distribution、deterministic chunk plan、content-addressed run、
  append-only registry、promotion/rollback、reference-fixed drift、batch inference lineage

## Dependencyと実装境界

新しいproduction dependencyは追加していない。CoreはPython標準libraryのSQLiteと、既存の
NumPy/SciPy/pandasで完結する。DuckDB、Arrow、Parquet、Numba/JIT、GPUは重要なAdvanced項目だが、
共有環境に偶然存在するpackageを使って「実装済み」とはしない。

PIT labはcontrolled revision fixtureを使う。これは既知真値でfuture revision exclusionを検査する
ための合成fixtureであり、実証データの代替ではない。pandas実装とSQLite window queryを独立に
照合し、decision時点より後のavailabilityを含めない。

## 検証

- focused B10 library tests: 12 passed
- B10 builder static/deterministic check: 6/6 passed
- top-to-bottom clean-kernel execution: 6/6 passed、error 0、execution count連続
- curriculum: B1–B10、Week 1–40連続
- full `quant_research` tests: 403 passed
- 60 Notebookのvalid Python・決定的JSON: pass
- Jupyter Book warning-as-error full build: pass

## 主張境界と次段階

- teaching fixtureによるdevelopment runはproduction modelではない。
- drift statisticはalert diagnosticであり、model invalidityやautomatic rollbackを単独で証明しない。
- row-oriented UTF-8 byte estimateはPython object、Arrow、Parquetの実memory/file sizeではない。
- B9 full candidate tournament、nominee manifest、locked outer一回評価は別milestoneとして未実行である。
- 座学教科書の残りはB11 Week 41–44。Stage 3とCapstoneはplaceholderで完成率の分母に含めない。
