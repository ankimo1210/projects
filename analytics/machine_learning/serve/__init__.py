"""Minimal serving layer for the textbook's Titanic pipeline (notebook 10).

Shows the last mile of an ML project: a persisted ``Pipeline`` exposed through a
real entry point (FastAPI service + CLI). Not installed as part of the library
package; run it from the project directory.
"""
