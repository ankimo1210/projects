title: 機械学習の実践 — 正しく問題を定式化し、検証し、解釈する
author: Kazumasa
language: ja
only_build_toc_files: true

execute:
  # Notebooks are committed with outputs already executed (see README).
  # Keep the book build fast and deterministic.
  execute_notebooks: "off"

sphinx:
  config:
    # require.js lets nbconvert-embedded Plotly figures render in static HTML.
    html_js_files:
      - https://cdnjs.cloudflare.com/ajax/libs/require.js/2.3.4/require.min.js

html:
  use_issues_button: false
  use_repository_button: false
