# Regenerated by scripts/build_report.py from _body.html + the shared tokens.
# report.html (the standalone deliverable) is tracked; this one only exists
# because the Artifact host supplies its own <!doctype> wrapper.
reports/report_artifact.html

# GiftEvalPretrain arrow downloads (~230 MB) and the index built from them.
_data/
