# 分類体系

分類の機械可読な正本は `config/taxonomy.yaml` です。設問形式、認知技能、WSET志向の内容領域、style-quality-price、階層的地理、主要品種を分離します。

初期分類はキーワードベースです。同じ設問に複数領域がある場合、`topic_primary` と `topic_secondary` を使い、人手審査で修正します。固有名詞は機械的に翻訳しません。
